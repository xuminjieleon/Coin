# CoinLens — 项目规范（AGENTS.md）

> 本文件是项目**规范与当前状态**的权威记录：目标、技术决策、架构、API 契约、运行环境、维护规则、待办。任何会话开始前先读本文件。
> 开发历史（每轮的背景/做法/回测数据/被否决方案/踩坑记录）在 **`DEVLOG.md`**——按需读取，不必每次通读；每次推进后必须同步更新本文件（当前状态/待办/契约变更）并在 DEVLOG.md 顶部追加记录。
> 面向用户的策略说明（说人话版：策略介绍/收益/风险/使用方式）在 **`STRATEGY.md`**——数字必须取自 DEVLOG 既有回测记录并保持同步，不得引入未验证数据。

## 1. 目标

做一个**本地个人使用**的加密货币交易分析助手（Web 可视化仪表盘），核心价值：
把机构交易员看盘的完整逻辑链条自动化呈现在一个页面——**价格结构(SMC) → 流动性 → 量价 → 衍生品资金流 → 综合结论评分**，辅助非专业用户做决策。

- 默认标的 BTC/ETH/SOL，支持动态搜索切换币安全部 USDT 交易对
- 分析维度三大支柱：聪明钱概念(SMC)、价格行为学(Price Action)、量价分析(Volume Profile/CVD)
- MVP 不含：AI 解读（二期）、股票市场（后续）、公网部署（本地使用）

## 2. 已确认的技术决策

| 项 | 决策 |
|---|---|
| 前端 | React 18 + TypeScript + Vite + **klinecharts v10**（SMC 标注用 overlay API）+ 纯 CSS（无 UI 框架） |
| 后端 | Python 3.12 + FastAPI + httpx + pandas/numpy，端口 8000 |
| 前端端口 | Vite dev 5173，已配 `/api` → `localhost:8000` 代理 |
| 数据源 | 运行时优先级链探测（见 §6），不写死任何一台机器的网络结论 |
| 部署 | 本地个人使用，无登录体系 |
| UI | 中文、深色主题（背景 #0d1117 / 卡片 #161b22 / 涨 #26a69a / 跌 #ef5350 / 强调 #2962ff） |

## 3. 系统架构

```
浏览器(React SPA, :5173)
  └─ REST /api/* ──→ FastAPI 后端(:8000)
                      ├─ services/binance.py       币安数据(主源+镜像failover+冷却)
                      ├─ services/binance_trade.py 币安合约签名REST(下单/撤单/仓位；主网/测试网，直连→系统代理)
                      ├─ services/gateio.py        Gate.io 衍生品(合约/期权/订单簿/contract_stats)
                      ├─ services/kline_cache.py   K线本地SQLite缓存(不可变历史+连续性检测)
                      ├─ services/derivs_store.py  衍生品历史持久化(Gate.io stats回填+快照+分位数)
                      ├─ services/microstructure.py 订单簿微观结构(深度/失衡/大单墙)
                      ├─ services/liquidations.py  清算数据(24h多空清算+分位+杠杆地图)
                      ├─ services/onchain.py       链上数据(mempool.space+blockchain.info)
                      ├─ services/macro.py         宏观联动(Yahoo日线+SQLite缓存+相关性)
                      ├─ services/scanner.py       全市场扫描(流动性前N跑引擎排序)
                      ├─ services/journal_store.py 交易日记(SQLite+计划重放+遵循率)
                      ├─ services/executor.py     自动交易引擎(计划→挂单→成交→beR→跟踪→时间退出；模拟盘/测试网/实盘三模式)
                      ├─ services/notify.py        PushPlus微信推送通道(markdown+重试)
                      ├─ services/notifier.py      每小时信号推送(整点+5min调度+计划指纹)
                      └─ services/analysis/        swings→smc→indicators→volume→decision
                                                    └─ context.py 决策管线共享层(API与推送同源)
```

> **数据模式**：不使用实时推送（无 WS）。统一为**手动刷新按钮 + 每 5 分钟自动刷新**（开关持久化 localStorage `coinlens.autoRefresh`），刷新时一次性重拉 analysis/derivatives/backtest 等，并把新 K 线尾部原地同步进图表（`syncBars`：时间戳等于最后一根→更新、更大→追加、更旧→忽略，不重置视图）。

布局：顶部 Header（快捷币种 BTC/ETH/SOL/BNB + 搜索/周期/刷新按钮/自动刷新开关/**全市场扫描按钮**/预警铃铛/更新时间）｜左侧 klinecharts 主图+SMC 标注+EMA+成交量副图（RSI/CVD 副图已于 2026-08-25 按用户要求移除；RSI/CVD 数据仍在决策评分与滚动回测中使用）｜右侧 360px 栏**分三个 Tab**：**决策**（**交易计划（置顶，可执行层优先）→决策摘要**→衍生品→成交量分布）｜**市场数据**（宏观联动→链上→订单簿→清算→事件日历）｜**交易**（组合风控→我的仓位→交易日记→**自动交易（2026-09-02 新增：模拟盘/测试网/实盘三模式+紧急全撤）**）。Tab 选择持久化 localStorage `coinlens.tab`。**响应式（纯 CSS 断点，无 JS 检测）**：≤880px 侧栏折到图表下方（图高 clamp(280px,46vh,460px)、侧栏全宽内部滚动）；横屏矮视口整页滚动；≤640px Header 折两行分组（header-main/header-controls）、刷新按钮/预警文字/更新时间前缀隐藏、搜索下拉右锚、toast/扫描弹窗近全屏、输入框 16px 防 iOS 聚焦缩放；Header.tsx 的可隐藏文字必须包在 `.btn-label`/`.ws-label`/`.brand-text` span 里。

## 4. API 契约（前后端共同遵守）

- `GET /api/health` → `{"ok": true}`
- `GET /api/klines?symbol&interval&limit&endTime` → `{symbol, interval, candles[]}`（纯 K 线无分析，供图表向左滚动加载历史分页；endTime 可选，返回其之前的一页；**走 kline_cache 本地缓存**，已缓存窗口直接命中不再请求币安）
- `GET /api/symbols?q=` → `[{"symbol","base"}]`（≤50 条）
- `GET /api/analysis?symbol&interval&limit&asOf` → `{symbol, interval, candles[], smc{swings, structureEvents, orderBlocks, fvgs, liquidityPools, premiumDiscount}, indicators{ema20/50/200, rsi14, atr14, adx14}(与candles等长含null), volumeProfile{poc,vah,val,bins}, summary{score(-100~100), bias, regime, keyLevels[], reasons[]}}, replay{asOf}|null}`（**asOf=回放模式**：K线截断到该根（含）、MTF 只用已收盘高周期 K 线、**funding/OI 加权组件只用当时已收盘日线（derivs_store.daily_rates）**、prevDay 为前一交易日——决策与回测口径一致无前视；响应带 replay 标记；patterns/factorContext 字段已删除，见 DEVLOG 12b 轮）
  - interval ∈ {1h,4h,1d,1w}（15m 已移除），limit 100~1000；评分 clamp ±100；bias: ≥15 bullish / ≤-15 bearish；reasons 中文按 |weight| 降序
- `GET /api/derivatives?symbol` → `{openInterest, openInterestValue, oiChangePct24h, oiHistory[], fundingRate, fundingHistory[], longShortRatio, longShortHistory[], takerBuySellRatio, source('binance'|'gateio'|null), options{atmIv, putCallRatio, contracts, expiry}}`（币安合约优先 → **Gate.io 回退**（futures tickers + contract_stats + contracts 规格 + options/tickers），任何字段可为 null）
- `GET /api/backtest?symbol&interval&limit&horizon` → `{samples, directionalSamples, ic, hitRate, scoreSeries[]}`（轻量评分 walk-forward：结构/EMA/RSI/溢价折价/CVD 背离按 regime 分化权重复算，IC=Spearman 相关；**采样窗口按周期固定**：1h/4h 2 年、1d 2 年（730 根）、1w 6 年（312 根），limit 参数已废弃；走 kline_cache）
- `GET /api/calendar` → `{events[{date,time,title,impact,kind}], note}`（本地维护 `backend/data/events.json`，事件时间按北京时区 authored；`upcoming_events(from_ms, horizon_ms)` 供 position 路由做事件预警）
- `POST /api/position/advise`（2026-08-22 新增；2026-08-25 两轮增强，见 DEVLOG）：body `{symbol, interval, direction('long'|'short'), entry, stop?, qty?, leverage?(1~200), openedAt?(ms)}` → `{price, pnlPct, unrealizedR, mfeR, maeR, barsHeld, scoreNow, scoreAtOpen, thesisState('strong'|'intact'|'weakened'|'broken'), eventsSinceOpen[{time,kind(structure|sweep|wyckoff),direction,text}], takeProfitLadder[{price,label,distPct,rMultiple}](盈利侧前4位), action{level(ok|info|warn|danger),text}(最优先纪律动作), levels{suggestedStop,beTrigger,trailStop,structureStop,liqPrice}, items[{level,text}], note}`。要点：**顺势/逆势检查与决策卡同口径**（500 根窗口+prevDay+MTF+资金费率/OI 加权）；**开仓时点决策回放**（需 openedAt：仅用开仓前已收盘 K 线+已收盘高周期+已收盘日线衍生品，无前视；评分漂移 ±25 触发 ok/warn；入场质量对照 PLAN_THRESHOLD）；**持仓期间事件**（结构 BOS/CHoCH 按最新事件方向汇总、扫流动性、Wyckoff；持仓早于 500 根窗口时自动拉长覆盖窗口 ≤3000 根，barsHeld 用时间戳精确计算）；**thesisState**=评分(×2)/最新结构事件/MTF 多数/CVD 背离带符号合计（描述性非预测）；**action** 优先级：缺止损>止损越强平>时间退出>证据转空且浮亏早离场>保本/跟踪执行>按计划持有；建议项另有：高周期背景与 CVD 背离对照、MFE/MAE+盈利回吐 ≥0.5R 警告+深浮亏后回升、结构止损参考（盈利中最近确认摆动低/高点）、止损紧贴未扫流动性池（≤0.5×ATR）插针风险、止损宽度校验（<0.8×ATR 紧/>3×ATR 宽）、跟踪容忍收紧一半（证据转弱时）、资金费率 carry（8h 成本/收入+本地历史分位+下次结算倒计时，需 qty）、48h 内高影响事件预警、+beR 减半仓+保本、剩余半仓跟踪止盈位（自持仓期 MFE 回撤 trail R）、时间退出窗口、名义/保证金/风险金额、强平风险（需 leverage>1）、波动率压缩提示。止盈阶梯只取**越过入场价的盈利侧**参考位（未扫流动性池/VAH-VAL/POC/区间极值/关键位，0.3% 聚类去重），盈利侧无位=真空提示。校验：stop 方向合法性 400、leverage 范围 422；热路径 ~4s
- **无实时行情 WS**：数据统一走手动刷新 / 5 分钟自动刷新
- `GET /api/orderbook?symbol` → `{symbol, source('binance_perp'|'gateio_perp'|'binance_spot'), mid, bestBid, bestAsk, spreadBps, topImbalance(前20档失衡-1~1), bands[{bandPct(0.1/0.25/0.5/1), bidUsd, askUsd, imbalance}], walls[{side,price,usd,distBps}](单档>同带中位5倍), levels, note}`——优先级链：币安官方合约 depth → Gate.io 合约聚合盘（quanto乘数换算USD）→ 币安现货镜像 depth（标注"现货盘"）；60s 内存缓存；快照口径
- `GET /api/liquidations?symbol` → `{long24hUsd, short24hUsd, total24hUsd, longShortRatio, percentileVsYear, history[{time,longUsd,shortUsd}]×48h, estimated[{leverage(10/25/50/100), longLiq, shortLiq}], price, source('gateio'|null), note}`——数据源为 Gate.io contract_stats 的 long/short_liq_usd 聚合（唯一免费源；真实逐笔 feed 需签名不可用）；Gate 不可达时多空清算如实置 null，估算强平位仍可用（=现价×(1∓1/lev) 隔离近似）；依赖 derivs_store 回填
- `GET /api/onchain` → `{btc{hashrate, hashrateChg30d, mempoolTxs, mempoolVsize, fees{fastest,halfHour,hour,economy}, difficulty{progressPct,difficultyChangePct,remainingBlocks}, activeAddresses, activeAddrAvg30d}, sources[], unavailable, updatedAt}`——mempool.space + blockchain.info charts；10min 内存缓存；交易所净流入/稳定币流向付费源不可达，如实置空
- `GET /api/macro` → `{series[{key(ndx/dxy/gold/vix/tnx/mstr/coin), name, last, chg1d/7d/30d, spark[45]}], correlations[{corr30/60/90, beta60}]（与 BTC 日收益 Pearson）, btc{last}, updatedAt, source}`——Yahoo chart API（**必须带浏览器 UA，否则 429**；全局 asyncio 锁 + ≥1.6s 间隔 + query1/query2 轮换 + 3 次退避重试；**403=封锁非限流，`_YahooBlocked` 快速失败不重试**——2026-08-27 起本网络直连 403，路由计划=直连双主机→系统代理双主机（VPN），全路由 403 进 15 分钟封锁窗并服务本地 SQLite 最后缓存日）；日线入 SQLite macro.db（不可变，仅尾部重拉）；响应 30min 内存缓存
- `GET /api/sources` → `{chains{数据类型→{order[],note}}, hostStatus{币安两主机+fapi|sysproxy 代理环节:{down,retryInS}}, systemProxy{url,down,retryInS}, note}`——数据源优先级链配置与实时冷却状态诊断（含 Windows 系统代理探测状态）
- `GET /api/scan?interval&top(10~80,默认40)` → `{interval, scanned, rows[{symbol,last,chg24h,quoteVolume,score,bias,regime,cvdDiv,hasPlan,topReason}], updatedAt, durationMs, note}`——24h ticker（币安官方合约→现货镜像）按成交额排序（剔除稳定币/杠杆币/UP-DOWN），每标的 200 根走 kline_cache 跑完整引擎评分（并发 6）；(interval,top) 结果 5min 缓存
- `GET/POST/DELETE /api/journal/trades[,/{id},/{id}/close]` → SQLite journal.db：`POST /trades`（快照 plan 可选，冻结开仓时几何）、`POST /trades/{id}/close {exit, reason}`（**平仓时用本地 K 线确定性重放计划**：止损→+beR减半保本→跟踪/目标→时间退出，同根 K 线保守盘口顺序=止损先判；planExit vs 实际 → adherence followed/deviated：同因或 ±0.5R 内=followed）、`GET /stats`（胜率/非亏损率/合计R/遵循率/分币种分周期；**盈利泄漏分析**：`byExitReason{reason:{count,sumR,avgR,winRate}}` 按离场原因拆解盈亏来源，`adherenceEv{followed,deviated:{count,sumR,avgR}}` 遵循 vs 偏离的均值差=偏离计划的 R 成本/笔——执行层优势是否兑现的直接度量）
- `POST /api/portfolio/advise`：body `{positions[...]≤20, accountEquity?}` → `{positions[{price,notionalUsd,riskUsd,liqPrice,unrealizedPct,unrealizedR,barsHeld,attention{level(danger|warn|info|ok),text}}], netUsd, grossUsd, marginUsd, totalRiskUsd, riskPctOfEquity, correlatedPairs(|corr|≥0.7), betas(vs BTC), items[]}`——组合层风控：净/总敞口、集中度>50%警告、两两相关性（本地 1d 90 日）、风险预算占权益 >3%warn/>6%danger；**每仓位 attention 分诊**（danger：无止损/止损越强平价/超时间退出窗口；warn：浮亏 ≤-1R 或逆势 |评分|≥25；info：评分轻度反向；ok：顺势）——评分走扫描器口径（200 根，无 MTF/衍生品上下文），items 汇总"N 个仓位需立即处理"；跨仓位不漏管退出纪律
- **derivatives 响应增强**：`topTraderRatio`（Gate.io top_lsr_size 大户持仓多空比）、`fundingHistory`、`historyStats{days, fundingPctl, oiUsdPctl, lsrPctl}`（相对本地持久化历史的分位数；days=时间跨度非行数）、`options` 扩展 `{rr25(25Δ call IV−put IV), maxPain{expiry,strike}, termStructure[]}`；每次调用快照入 derivs.db snapshots 表，`ensure_backfill` 按优先级链回填（Gate.io contract_stats 1d×1000+1h×720 含清算USD → 币安 futures/data 无清算、毫秒→秒归一化；6h 增量刷新，同符号并发等待防部分读）；**多源按列 UPSERT 合并**（NULL 不覆盖）
- **analysis 响应增强字段**：`smc.orderBlocks[].quality / fvgs[].quality`（0-100）、`smc.sweepEvents[]`、`indicators.cvd[]`（累计主动买卖差，K 线 takerBuy 字段）、`volumeProfile.pocSeries[]/developingPoc`（滚动 POC）、`wyckoff{phase,events[]}`、`volatility{atrPct,bandwidthPct,squeeze,state}`、`cvdDivergence`、`mtf{list[{interval,score,bias,cvdDiv}],alignment}`、`summary.tradePlan{direction,entry,stop,target1(null=跟踪止盈),beTrigger,beR,targetR(null),scaleOut,trailR,stopAtr,depthAtr,texitBars,fillBars,rr(null=跟踪),note}`——**几何按周期分化**（PLAN_GEOMETRY，第 13 轮 5 年重校准 2026-08-25，见 DEVLOG 第八轮）：1h 0.5×ATR 回踩/2.0×ATR 止损/+0.15R 减半+保本/目标 0.5R/96 根退出；4h 0.75/1.0/+0.75R 减半保本/0.35R 跟踪止盈无固定目标/48 根退出/18 根成交窗口；1d 1.0/1.2/0.5R/0.35R 跟踪/12 根/9；1w 0.75/1.5/0.5R/0.75R 跟踪/24/8（1w 未参与重校准，沿用第 11 轮）（**PLAN_THRESHOLD**：4h/1d/1w=|score|≥10、1h=25；校准依据与盲测数据见 DEVLOG 第 10/11/13 轮）（第五十六轮的"波动率极值追单拦截"上线后同日被 19 币回测数据证伪为净负收益、用户拍板撤销、代码已回退——tradePlan 生成不含任何 regime 拦截，见 DEVLOG 第五十六轮）
- `GET/POST /api/notify[,/test]` → 微信推送（**双通道可切换**：`channel='wecom'`（企业微信群机器人 webhook，免费无限制、免实名，消息收在「企业微信」App，**默认**）｜`'pushplus'`（免费 200 条/天但**需官网实名认证否则 905**，消息收在微信本体））：`GET` → `{enabled, mode('events'|'brief'), channel, heartbeat, symbols[], intervals[](1~4 周期，2026-08-27 起多周期), tokenSet, tokenMasked, wecomKeySet, wecomKeyMasked, lastRun, nextRun, lastError, recent[10], planStates{...}}`；`POST /api/notify` body `{enabled?, mode?, channel?, heartbeat?, symbols?(1~20，2026-09-02 第五十二轮由 10 扩到 20——11 币超旧上限), intervals?(1~4 如 ["1h","4h"]；旧版单值 interval 仍接受自动转列表), token?, wecomKey?(完整 webhook URL 或裸 key 均可)}` 更新配置（校验 400；**任何变更重置计划指纹播种**防假事件风暴）；`POST /api/notify/test` 立即按当前通道推测试消息（无凭证 400）。**调度**：整点+5min 对 symbols×intervals 并发跑 `services/analysis/context.run_analysis`（与 /api/analysis 同一份代码；**2026-08-29 第三十一轮 B 方案：全部周期只收盘口径**——asOf=最近已收盘 bar 开盘时间（1w 周一对齐），计划状态只在 bar 收盘后变化：4h 事件严格至多每 4 小时一次、1h 去掉 5 分钟形成中尾巴、推送与图表回放/journal 重放同一决策口径，形成中 K 盘中闪烁（第二十四/二十六轮伪象）不再触发推送；返回的最后一根 K 不是目标 asOf bar 时该对按本轮失败处理、指纹不变——绝不拿缺页数据比指纹）；**指纹=symbol|interval→direction**（新/转向/消失事件）；**改单事件（2026-08-31 第四十六轮新增；2026-09-01 第四十八轮扩阈）**：方向不变但 entry/stop 相对**上次实际推送价**漂移 **≥40%×单笔风险 且 ≥0.5%×价格**时推【改单】（带旧价→新价+完整计划块；原 20% 单阈值 30 天回放日均 67 条、78~90% 在基线后 1~2 根 bar 触发=计划呼吸噪音，扩阈后 ~30 条/天、触发时价格漂移中位 ~1%，依据 `tests/amend_freq_stat.py`），持久化 `pushedPlans{key→{entry,stop,direction}}` 记录上次推送价作漂移基准（首轮静默播种不进 pushedPlans——用户无挂单可改；计划消失/配置变更时清除对应键）；【消失】/【转向】带显式撤单指令并**显示原挂单价**（"原挂单：入场 X / 止损 Y"方便在交易所定位那笔待撤单，事件生成时先于 pushedPlans 清除抓取）——**BNB 08-31 凌晨取消后残留挂单幽灵成交 -1R 的教训**（第四十五轮追加2）；持久化 `data/notify.json`（**2026-08-28 第二十七轮起随仓库提交**——用户决定把企业微信 key 进仓库作各机器引导配置，repo 为私有；各机运行后本地状态（指纹/seeded）自行演化，git pull 冲突时保留本地文件；旧单周期配置自动迁移并静默重播种）；events 模式仅推【新】/【转向】/【消失】/【改单】聚合消息（消息带周期标签如"BTCUSDT 4h 做多"；首轮静默播种）；**心跳（2026-09-01 第四十九轮，配置 `heartbeat` 默认开，可 POST /api/notify {"heartbeat":false} 关）**：events 模式一轮无事件时推"CoinLens 无变化 HH:MM"（一行无变化说明+本轮分析失败键如实标注⚠️；首轮静默播种不推、全部分析失败沿用既有快速失败不误报"正常"、有事件时只推事件）——用户确认服务活着；brief 模式每小时全量简报（本身必有消息无心跳）——**消息格式（2026-09-02 第五十三轮改版，用户手机预览三候选拍板"结构化色块"——企微机器人 markdown 支持集无真表格）：events=灰色计数摘要行（N 新 · N 转向 · N 改单 · N 消失）+ 逐事件色块【事件】币 周期+颜色方向词（绿=做多/橙红=做空）+计划行进引用块（`> ` 前缀）紧凑两列（入场/止盈｜止损/减半保本/跟踪）；改单旧行内价（入场 X（旧 Y））；消失/转向带红色撤单警示行+原挂单价；brief 同块样式保留 ---------- 分隔与观望列表（第二十八轮定版）；`_plan_lines/_exit_lines` 单源 events/brief 共用；颜色 font 标签在非 wecom 通道（pushplus）发送前剥除**；**分段发送（2026-09-04 第六十二轮）**：企微 markdown 单条 4096 字节上限（00:05 轮 40058 整条被拒事故），`notifier._send_chunked` 超 3800 字节按 `\n\n` 事件块贪心装包、单块超限按 UTF-8 字节硬切（不破多字节）、标题随首条（后续条带"（N/总）"序号）、条间 0.4s、单条够用走原路径零变化；**遗留：指纹防丢未做**——发送失败轮指纹已标"已推送"，该批信号不重发（用户拍板，00:05 批次漏看为实例）；消息只含交易操作的实际价格（入场/止盈/止损/减半保本价——**2026-09-02 第五十二轮用户要求：止盈行先于止损行**，`notifier._plan_lines` 单源、events/brief 共用；跟踪制周期 4h/1d/1w **直接打印跟踪距离与半仓离场时的具体止损位**——journal 重放口径 stop=max(入场价, 最高价−trailR×风险)，2026-08-28 第二十八轮起，用户无需换算；旧"跟踪启动价"已弃用——它在 4h/1d 上低于实际启用位、有误导），无 R 倍数无评分；分析失败的标的/周期对不参与指纹比对（防假"消失"）；disabled 时仍每小时刷新 planStates 供预览；**入场后仓位管理不推送**（回测口径=开仓时计划冻结，管理位走 App 仓位面板，见 DEVLOG 第十七轮）

- **`GET/POST /api/executor[,/test,/panic]`（2026-09-02 第五十四轮新增）自动交易**：`GET` → `{enabled, mode('paper'|'testnet'|'live'), testnet, dryRun, confirmLive, keysSet, apiKeyMasked, instance, symbols[], intervals[](⊆{1h,4h}), riskPct, leverage, maxConcurrent, maxNotionalPctPer, maxGrossNotionalPct, dailyLossLimitR, equityUsd(模拟盘定仓基准), postOnlyEntry, pushEvents, paused, todayRealizedR, reconciled, positions[{key,state(pending|open|closed),entry,stop,filled,beQty,beDone,awaitingEntry,beTrigger,target1,trailR,barsHeld,...}], closedCount, closedSumR, lastPlanRun, nextRun, lastError, events[30], routes, note}`；`POST` body 为配置补丁（校验 422/400：riskPct 0.1~15（paper/testnet 沙盒按用户 2026-09-02 确认的 15% 跑——f 阶梯审计 1h+4h RUIN@17.5%、15% 对应 DD ~94%，**实盘硬闸 ≤3 按"补丁合并后的最终模式"校验**（拆包请求绕不过；实测带 15% 切实盘被拒）、intervals ⊆ {1h,4h}、symbols ≤20、进入实盘的请求须自带 confirmLive=true+已配密钥、离开 live 自动重置 confirmLive、equityUsd 仅模拟盘可设）；`POST /test` 连通/密钥/持仓模式检查（单向模式校验失败明示）；`POST /panic` 需 `{"confirm":"PANIC"}`——**先原子停用落盘再逐仓 try/except**（撤全部本机挂单+市价平全部仓位；失败仓大声报警留手动处理），返回 `{ok, closed, errors[]}`。**语义（第五十五轮加固；第六十轮币安条件单迁移适配）**：决策=与推送/回放同一收盘口径（**共享 `context.closed_bar_analysis`/`last_closed_open`/`notifier.drift_material`/`journal_store.trail_stop_level`+`bars_held_since`——四处语义单源，勿再本地手抄**）；**止损单走 Algo Order API（第六十轮）**——币安已把条件单（STOP_MARKET 等）迁出 `POST /fapi/v1/order`（测试网 -4120 拒收实测、主网 algo 端点已上线），`LiveBroker` 按 coid 角色字母路由（S=止损→`place_algo_order/cancel_algo_order/get_algo_order`，algoStatus TRIGGERED/FINISHED→FILLED 按 actualOrderId 查成交价、CANCELED/EXPIRED→None 走补挂自愈；`open_all` 合并 openOrders+openAlgoOrders 供对账）；计划→post-only(GTX)限价入场单+reduce-only 止损单**同时挂**（止损先挂且 coid 即时落库；**任何下单异常先按 coid 探针查证**——已送达绝不误判为未下单；非 GET 传输错误不盲重试防双重提交；入场被拒转 awaiting 择机重挂）；成交后按**冻结计划**管理（+beR 半仓止盈→止损提到入场价→跟踪(仅已收盘bar)/目标→时间退出，30s 巡检**并行**+止损单缺失即补+改单先挂新再撤旧+部分成交收养先挂新止损）；pending 响应 消失/转向/改单(共享漂移阈值)/fillBars 到期；持仓期间同 symbol|interval 新计划忽略（回测口径）；**实网余额获取失败拒绝新开仓**（不按猜测权益定仓）；**模式切换前清理全部旧环境仓位记录（带真实持仓拒切），防旧冻结计划当真单挂到新环境**；**place_failed 同 bar 熔断（2026-09-03 第六十一轮）**——place_failed 仓位行为 closed 态、`_active_positions` 不可见，导致每个计划周期重挂同一单（BCH 单日 -4120 失败 21 次事故）；`_try_place` 下单前查当前计划 bar 窗口（`last_closed_open` 锚点）内同 key+direction+tick 精度 entry/stop 是否已有 place_failed 行，命中则熔断至下一 bar。**第六十三轮加固（2026-09-05"只有两笔订单"事故：探针假阴性→两笔裸仓+-2019 保证金风暴）**：①探针二元化 `_probe()`——探针自身失败（网络抖动）一律"本轮不动、保留跟踪"，只有干净 -2013/None 才判消失（入场/止损/保本/目标/撤单前五处存在性判断全改；旧代码把探针异常当"订单不存在"，BTC 挂单 19 秒后被抹掉跟踪、后来成交成无止损裸仓，XRP 部分成交同洞）；②**保证金钳制**——`_try_place` 在名义额上限后按 `usdt_available()×leverage×0.95` 二次钳 qty、低于 minNotional 干净跳过留 warn（15% 风险÷~2% 止损=名义额需求 ~7.5×权益→恒撞 -2019；实盘 3%×8 并发同样会撞，属实盘级隐患）；③place_failed 撤止损改 `_cancel_verified`（撤+查证×2，仍在场大声事件——FIL 残留止损反堵 -4067 事故）；④**30s 孤儿清扫** `_sweep_orphans`——`open_all` 凡 `cl{instance}*` 前缀且 pid 行非 pending/open 的挂单一律撤掉记事件（他机标签/手动单不碰；在途挂单行永远先落库故无竞态；无主净仓位大声报警不自动处理）。⑤**按止损距离推导每仓杠杆（2026-09-05 第六十四轮；用户要求的平 75/100x 被数字否决——逐仓清算价 ≈1/杠杆 会钻进 1h 止损距离 1.3~1.9% 内侧、交易所清算引擎取代计划止损、插针即爆）**——`_derived_leverage=1/(2×止损距离+1.2%MMR缓冲)`，cfg `leverage` 为上限（测试网现 100）；紧止损 ~19-25x、宽止损（ZEC 4.9%）~8x，逐仓清算价永远 ≥2×止损距离外；每仓保证金 ≈1.5~3R、8 并发全额 ~3-4k；`ensure_setup(sym, lev_eff)` 与保证金钳制共用推导值；管理期止损只收紧 → liq 相对更远，开仓后无需重设。⑥**保本止损提位自愈（2026-09-05 第六十五轮，SUI -2021 事故）**——`_on_be_filled` 的 `_replace_stop` 失败（如插针成交后价格弹回入场价上方、保本止损单会立即触发 -2021）不再吞掉保本事件/推送；`_sync_one` 每轮检查"be_done 且止损仍在入场位亏损侧"→ 30s 重试提位，优于入场位（跟踪已收紧）不误触发。**护栏**：单向持仓校验、并发上限默认 8、同币跨周期重叠第二单风险减半（共享预算）、名义额 300%/800% 为保证金后置上限、日亏 −6R 停新开仓、杠杆=cfg 上限+逐仓+按止损距离推导（见⑤）。**三模式**：paper（默认，真实信号+1m 真实行情模拟撮合，无需 key）/testnet（testnet.binancefuture.com）/live（三重武装）。**执行器必须单机运行**（clientOrderId 带机器标签，他机挂单警告不接管；双机冗余推送哲学不适用）；`data/executor.json` **gitignore（含 API secret，永不提交）**、状态 `data/executor.db`（**启动失败不连累主服务**：main.py 有 try 守卫）；事件走 notify 通道推送（【挂单】【成交】【保本】【平仓】【撤单】【紧急全撤】）。单测 `tests/executor_test.py` 87 checks（第六十五轮新增 T20 提位自愈 7 项；第六十四轮 T19 推导杠杆 7 项；第六十三轮 T15~T18 十二项）

## 5. 当前状态

### 说人话总览（先读这个，技术明细见下）

**产品能用了吗**：能。打开网页就是完整看盘台——K 线图（自动标注结构/扫流动性）、决策评分卡（-100~+100 分+理由）、交易计划卡（挂单价/止损/止盈/减半保本，照抄去交易所挂单即可）、衍生品/宏观/链上/订单簿/清算/事件日历、全市场扫描、仓位建议、组合风控、交易日记（自动统计你"照计划做 vs 自己乱做"每笔差多少）。手机适配已上线，外网访问走 Cloudflare Tunnel。

**策略到底赚不赚钱**（回测口径、未计费）：9 个主流币 × 5 年 × 四个周期，每个币合计 +561~+910R，**每币每一年都赚**（含 2022 大熊市）。1R=每笔准备亏的钱，按单笔风险=本金 1% 换算，五年毛利润 ≈ 本金的 +561%~+910%（未复利）。方向猜对猜错接近抛硬币——钱靠"等回调才进、错了亏小钱、对了先保本、利润让它跑"的执行纪律赚，这个优势在几十个币、A 股、美股上都复现过，是结构性的、不挑市场。

**实盘要打几折**：费用双边 0.10% 时利润保留 1h 58% / 4h 88% / 1d 96%——1h 必须挂单（maker）入场，账面利润还要再按下界口径缩水（BACKTEST §1 口径说明）；复利表里"几千倍"是数学上限不可当真，最可实现的复利口径是 1d：f=1% 净费五年 53.6×、年化 +179%（9 币，BACKTEST §4.1；四币口径 8.1×/+72% 作保守参照）。

**美股/A 股**：都验证过"可行"但**都没接进产品**，要不要做等你拍板——美股 15 只大盘全历史 +1205R（44/45 个年份盈利，已定专用参数）；币圈概念股 6 只（2020-08 起）固定注额 +152.9%、年年赚，但"傻拿"更赚（MSTR +929%，代价 89% 回撤）——要稳还是要暴利是选择题；A 股 12 只 ETF +143%（2018 年最深）。

**微信推送**：企业微信通道，每小时把 11 币 × 1h/4h 的新计划/计划转向/计划消失/**改单（entry/stop 漂移）**推到手机，只认收盘数据，消息只含实际挂单价；【消失】/【转向】带显式撤单指令。已双机运行。

**自动交易（2026-09-02 新增）**：交易 Tab 最下方的面板，把推送的计划直接接到币安合约自动执行——出现计划自动挂限价入场+止损，成交后自动按计划管理（减半保本→跟踪→时间退出），计划变了自动撤改单。**默认模拟盘**（不用配 key，真实信号模拟撮合，先看它怎么执行）；接下来按拍板走币安测试网空跑 2~4 周再切实盘（实盘要三重确认+密钥只开合约交易绝不开提现）。安全底线：止损单永远挂在交易所上，电脑关机/断网仓位也不裸奔；日亏超过上限自动停止新开仓；有一键"紧急全撤"。**单笔风险已按你确认设为 15%**——f 阶梯审计（第五十一轮）实测 1h+4h 爆仓门槛 17.5%、15% 对应 DD ~94%（普通的三币同夜齐止损=一夜 −45%），所以 15% 先在模拟盘/测试网用假钱跑给你看；**实盘 ≤3 硬闸保留**（按补丁合并后状态校验，拆包绕不过），想在实盘突破须拿测试网亲眼看到的回撤数据重新拍板。**币安条件单迁移事故（2026-09-03 第六十轮）**：币安把止损类条件单（STOP_MARKET）迁出旧下单接口、测试网强制拒收——空跑首日 412 次下单全死在止损步、交易所零挂单（你看到的"只有两条订单"）；止损单已全链路改走 Algo Order API 并实测打通，同秒竞态漏出的无止损 ARB 裸仓已市价平掉、竞态已修（重挂入场必须锁内确认仓位行活着且止损在场）。**探针误判事故（2026-09-05 第六十三轮）**："只有两笔订单"的真相=两笔无人管理的裸仓（挂单后 19 秒巡检撞网络抖动被误判"不存在"抹掉跟踪、后来成交）+它们锁死保证金后所有新入场单被币安 -2019"保证金不足"连环拒掉（推送照常、币安零新单）；四处修复（探针失败不动/保证金钳制/撤止损验证/30 秒孤儿清扫）+清账后 08:05 周期实测 4 个空单全部"入场+止损"成对挂上。**已拍板（2026-09-05）**：6,005 USDT 测试网钱包装不下 15%×8 并发，选③ testnet riskPct 降到 3；15% 时代的 4 张缩仓挂单已退役（孤儿清扫自动撤单），下周期起按 3% 定仓。**杠杆已改"按止损距离推导"（2026-09-05 第六十四轮）**：你要的"加大杠杆省保证金多开仓"以安全版落地——每仓杠杆高到清算价永远在止损 2 倍距离之外（紧止损 ~19-25x、宽止损 ~8x，100x 只作上限），每仓保证金降到 ~1.5-3R、8 张全额仓装得下；平的 75/100x 因清算价钻进止损内侧（插针即爆、退出结构变形）被数字否决。BNB 首张成交单已在真实测试网走完"成交→减半保本→止损提保本→挂目标"完整管理链。

**追高拦截（2026-09-02 第五十六轮上线 → 同轮看回测数据后撤销，DEVLOG）**：曾按你拍板加过一条拦截——波动率极值+价格贴区间顶/底 10% 时顺势计划不生成。同日用 19 币×5 年两臂对照回测量了代价：4h 几乎无感（5 年只拦 3 笔 +0.7R），**1h 拦掉了 10608 笔胜率 98.4% 的赚钱单、代价 −1209R（约 1h 五年利润的 14%）**——拦截是净负收益的规则。你看到数据后拍板"不拦截"，已回退到无拦截代码。教训归档：这类 regime 闸门（第四十四轮贴池过滤、第二十轮波动率分闸）已第三次被数据否决——波动率极值下"贴极端的顺势单"在这个系统里大多是赚钱的，不是亏钱的。

**已知短板（诚实）**：方向胜率天花板 ~60%（多轮验证）；1w 周期样本太小不可靠；清算/订单簿是快照非实时；宏观数据要开 VPN；参数已冻结不再调（防过拟合，无新数据不重开）；**CVD 信号跨机口径漂移**（2026-09-03 第五十八轮发现：币安合约/现货镜像的主动买入量口径不同，数据源降级时最强信号 CVD 共振会漂移甚至反向，待修）。

### 已上线功能（技术记录，说人话总览见上）
- **后端**：SMC 决策引擎（swings→smc→indicators→volume→decision，regime 分化权重+MTF 共振+CVD 多周期共振+funding/OI 加权+Wyckoff+波动率状态）、K 线本地缓存、衍生品持久化与分位、订单簿微观结构、清算聚合、链上、宏观联动、全市场扫描、交易日记（计划重放+遵循率）、组合风控、仓位建议（决策卡同口径+回放+事件+证据状态+止盈阶梯+action）、K 线点击回放（asOf）、**自动交易执行器（2026-09-02 第五十四轮上线、第五十五轮六轨审查加固：binance_trade 签名客户端+executor 引擎三模式，与推送/回放同口径且语义单源共享，4 CRITICAL+10 WARNING 已全修（下单探针/对账死锁/模式切换幽灵单/panic 隔离等），57 项状态机单测+真实管线 smoke 通过——细节与安全设计见 §4 executor 契约与 DEVLOG 第五十四/五十五轮）**。SMC 单测通过；各端点实测 200、校验 400 正常
- **前端**：全部面板就绪（DecisionCard/TradePlanCard/DerivativesPanel/VolumeProfilePanel/MacroPanel/OnchainPanel/OrderBookPanel/LiquidationPanel/CalendarPanel/PortfolioPanel/PositionPanel/JournalPanel/ScannerModal/MtfBar/SourceHint）；**移动端响应式适配已上线**（App.css 断点见 §3 布局；宏观表格改为 4 列两行自适应，修复了桌面端 360px 侧栏下走势列被裁切的历史问题）；`npm.cmd run build` tsc 零错误
- **加载性能（2026-08-25 第十二轮）**：后端 binance/gateio 共享 AsyncClient 连接池（勿在 `_fetch/_get` 里改回每次新建 client）；derivatives 5 个币安调用/Gate.io 两路/analysis 衍生品上下文均为 asyncio 并行；`ensure_backfill` 走后台任务（强引用防 GC）；前端各面板与 analysis 并行刷新。整页 ~15s→~2.4s（热 0.5s）。CPU 非瓶颈（full_analysis 500 根 13ms），勿用多进程处理请求路径
- **连接池加固（2026-08-27 第十五轮）**：共享 client 经 `httpx.Limits(keepalive_expiry=60)` 剔除闲置连接（**坑：httpx 0.28 已移除 transport 的 keepalive_expiry 参数，只能走 Limits**）；`_fetch/_get` 遇传输错误（ConnectTimeout 除外——从未连上=真不可达，非僵死连接）用一次性新 client 重试一次、成功则 `_swap_client` 换池——防御长跑进程连接池僵死（08-25 启动的进程跑 1.5 天后对可达主机全部请求失败的事故，重启+此加固解决）；勿改回每请求新建 client，错误路径的一次性重建是防御逻辑
- **系统代理（VPN）接入数据源链（2026-08-27 第十六轮）**：新增 `services/sysproxy.py`——运行时读注册表探测 Windows 系统代理（60s TTL；VPN 客户端通常以系统代理模式工作，httpx 不读 WinINET 设置只认环境变量），传输失败 300s 冷却快速失败，共享代理连接池带同样的 keepalive 僵死防御；**币安链插入"同主机经系统代理"环节**（binance.py `_get`/`get_depth`，独立冷却键 `fapi|sysproxy`——直连失败不阻塞代理尝试、反之亦然），**宏观 Yahoo 路由计划=直连双主机→代理双主机**（403 快速失败保留；直连 403+代理不可用=全路由封锁→15 分钟快速失败窗防每个序列重复撞墙）。代理是**链中一环非全局开关**：VPN 关闭自动回退既有降级链（镜像/Gate），零配置切换。`/api/sources` 新增 `systemProxy` 状态与各链代理环节描述
- **盈利扩展三杠杆已接线（2026-08-25）**：①**机会捕捉**——预警铃铛开启时每次刷新后台拉 `/api/scan`（服务端 5min 缓存）喂计划观察器：市场级**新计划/计划转向**推送（首个周期静默播种防风暴、每标的 30min 冷却、toast 点击切标的）+ 当前标的**回踩接近计划入场区**（≤0.3×ATR）与**挂单窗口到期**提醒（key=symbol|interval|direction，entry 随 ATR 漂移原地更新不重置计时，到期每窗口提醒一次）；②**组合分诊**——PortfolioPanel 每仓位 attention chip（紧急/注意/偏逆）按严重度排序置顶；③**遵循率成本**——JournalPanel 显示遵循 vs 偏离的均值差（偏离成本 R/笔）+ 按离场原因的盈亏拆解
- **每小时微信推送已上线（2026-08-27 第十七轮；2026-08-27 第二十二轮扩展多周期；2026-08-29 第三十一轮改只收盘口径）**：`services/notify.py`（PushPlus/企业微信双通道）+ `services/notifier.py`（整点+5min 调度、计划指纹、events/brief 双模式、**asOf=最近已收盘 bar 的回放语义分析**）+ `routers/notify.py`（配置/状态/test API）；决策管线抽取为 `services/analysis/context.py` 供 API 与推送同源复用（analysis/position 路由已切换，行为零变化，回归通过）；**多周期（第二十二轮）**：配置 `intervals` 列表（1~4 周期，当前 ["1h","4h"]），指纹键 `symbol|interval`，消息带周期标签，planStates 按 interval 嵌套，旧单周期配置自动迁移；消息只含交易操作实际价格；**入场后仓位管理不推送**（口径见 DEVLOG 第十七轮）
- **1h+4h 叠加回测（2026-08-27 第二十二轮，DEVLOG）**：`tests/stacked_mtf.py`（多进程 8 worker；5y 记录缓存已按当前源码哈希重建）——四币 5 年：仅1h +2472.0R/DD4.0R，1h+4h 全额叠加 +3992.6R(+61%)/DD6.9R/并发峰值8仓，**共享预算口径（同币重叠时段各半仓）+3855.8R(+56%)/DD6.8R**——半仓纪律只损失 3.4% 利润；每年叠加均>仅1h（2022 +843.9 vs +502.3R）；同币 1h/4h 仓位重叠率 58~64%、重叠对同向 71~76%；未计手续费。用户结论：叠加划算，风险纪律用共享预算（注：该轮基线系企业网机器数据口径，本机复现第 13 轮权威口径为 1h +2294.4R，机器差 ~7%，见第二十三轮校验）
- **手续费/滑点敏感性 1h vs 4h（2026-08-28 第二十三轮，DEVLOG）**：`tests/fee_compare.py`（口径同 backtest_5y，feeR=双边费率×entry/risk；本机 `_5y_cache_*` 已按当前源码哈希重建可复用）——笔均费用双边 0.10% ≈0.06R（止损距离中位 1.77%/1.84%，两周期绝对距离相当），1h 笔数 4× 而 EV/笔 仅 4h 的 39% → **费用吃掉 1h 边际的 39~47%、4h 仅 14~17%**；四币 5 年总利润：单边 0.05% 下 1h +1393.4R 仍领先 4h +1273.9R，**单边 0.06%（双边 0.117% 交叉点）起 4h 反超**（+1232.3 vs +1213.2R）；maker 入+taker 出（双边 0.07%）1h 仍领先 25%——**1h 存活关键=入场挂单 maker**；1h 非亏损率费后 96.9%→79.7%（双边 0.10%）/71.6%（0.12%），4h 几乎不动；叠加结论对费用鲁棒（双边 0.10% 下仍 +91%）；零生产改动
- **方向拆分 + 2026H1 切片（2026-08-28 第二十五轮，DEVLOG）**：`tests/direction_split.py`（harness 同 fee_compare，每笔保留 direction；1d 缓存已重建为当前哈希，1w 仍过期）——**做多/做空基本对称**：1h 多/空 EV +0.167/+0.164R、4h +0.440/+0.415R（差 <6%），空单占比 57~61% 且总 R 更高纯属笔数；1d 多头略优（+0.452 vs +0.372R）；**三周期两方向逐年全为正**（含牛市年的空单）——执行层优势双向成立；2026H1（UTC 1~6 月）：1h +259.9R 毛/+157.2R 净@0.10%、4h +176.9R/+154.6R（EV +0.519R 超其 5 年均值）、1d +18.2R，合计毛 +454.9R/净 +329.1R，**逐月全正**；**2025 全年**（`--year` 切片）：1h +458.6R/4h +297.7R/1d +55.7R，合计毛 +812.1R/净 +571.5R，12 个月逐月全正、方向对称、分币均衡；未建模资金费率 carry（正费率时空单实盘另有收益）；零生产改动
- **复利口径回测（2026-08-28 第二十九轮，DEVLOG）**：`tests/compound_backtest.py`（harness 同 fee_compare；事件账户=开仓按当时已实现权益快照 f、平仓结算滚入；1h+4h 共享预算叠加复用第二十二轮 scale 规则）——**确认既往全部报告为固定注额非复利（ΣR 线性）**；复利结果是数学上限不可当真：1h f=1% 净@双边0.10% 5 年 154 万×（容量/市场冲击/模型外风险/费率敏感，末期单笔名义额远超市场深度）；**最接近可实现的是 1d：f=1% 净费 5 年 8.1×、年化 +72%、已实现口径 DD 4.0%**（仍未含持仓中浮亏）；实盘介于两口径之间（定期再平衡注额）；复利增长率对费率近线性敏感（1h 毛/净差 4 个数量级）。**追加月化/季化拆解**：194×/年系 CAGR 几何均值非单年成绩（实际分年 ×24~×387、2023 最弱 ×69）；共享预算 f=1% 净月度 60 期 100% 正、中位 +53%/最差 +15%/最好 +127%，季中位 +268%，平滑折算月 ×1.55/季 ×3.73；仅1d 月中位 +5%；单仓名义≈0.54×权益（止损距离中位 1.84%）——**容量在几百万~几千万 USDT 权益量级把复利压回线性区**（1 万起步轨迹：2022 年末 ≈1538 万/单仓 ≈836 万 → 2023 年末 ≈10.6 亿/单仓 ≈5.8 亿，机器输出），"100% 月份为正"本身即模型过乐观信号。零生产改动；本轮把 1h/4h/1d 记录缓存统一重存为 backtest_5y 的 window 键（stacked_mtf 旧键缺 window 不兼容的坑已修，`load_records` 已统一）
- 仓位按 symbol 持久化 localStorage `coinlens.position`，数据刷新时自动重新分析
- **LTC 纯样本外回测（2026-08-25 第六轮，DEVLOG）**：`tests/backtest_ltc.py` 复用第 11 轮 harness 跑生产配置（LTC 从未参与调参）——1h +116.6R(98.7%)/4h +150.4R(87.0%, EV+0.301R)/1d +24.5R(86.5%)/1w +1.1R(19 笔小样本)；方向准确率 <50% 而利润为正，执行层优势在第四个标的上复现；未改任何生产参数
- **用户 Pine 脚本对比回测（2026-08-26 第十四轮，DEVLOG）**：`tests/backtest_pine.py`（每 symbol 一 worker 并发）忠实复刻用户三个 TradingView Pine 脚本并与生产 1h/4h 策略在同一 5 年窗口/同一费率下对比——生产策略 MAR 28~71（1h）/15~25（4h）vs Pine 0.8~2.6、回撤 3~4%（1h）/5~6%（4h）vs 21~38%、逐年全正（Pine 单笔 EV 更高但笔数少 17 倍且 BTC 有亏损年）；4h 单笔净 EV +0.36~0.41R 约为 1h 的 3 倍但权益回撤更大、非亏损率更低——1h 曲线更稳、4h 执行可行性更高；CoinLens 侧直接复用 `_5y_cache_*` 记录缓存零重算；无生产代码改动
- **成交量前 10 回测排名（2026-08-25 第七轮，DEVLOG）**：`tests/backtest_top10.py`（CONF 复用 backtest_ltc）——BTC +321.9R > ETH +314.1R > BNB +305.3R > SOL +303.1R > SUI/DOGE/ZEC/PYTH ~+275~283R > XRP +263.4R > TUTU +138.8R(历史短)；**10/10 币种全部盈利、37 个币种-周期组合 EV 无一为负**，1h EV 收敛于 +0.09~0.11R、4h +0.25~0.38R——跨币结构性优势；纯样本外 BNB 落在调参标的区间内（无明显币种偏斜）；未改任何生产参数
- **五年扩展回测 + 第 13 轮几何重校准已采纳（2026-08-25 第八轮，DEVLOG/BACKTEST.md）**：`tests/backtest_5y.py`（BTC/ETH/BNB/SOL × 1h/4h/1d 各 5 年 + 1w 全历史）——旧几何 5 年基线四币合计 1h +1166.2R / 4h +1062.1R / 1d +145.8R / 1w +52.5R 且逐年全正；按 §7.3 新数据条款预登记协议（A 调参/B+C 盲测 + A+B 重调/C 盲测两阶段、单遍坐标下降、1w 排除）重校准 1h/4h/1d 几何并全部通过 C 段盲测（+99.8%/+49.2%/+38.9%，四币逐个改善），**PLAN_GEOMETRY 已更新**（1h 0.5/2.0/0.15R/0.5R 目标；4h 0.75/1.0/0.75R/0.35R 跟踪；1d 1.0/1.2/0.5R/0.35R 跟踪/12 根），1w 与阈值/成交窗口不动；SMC 单测通过、评分路径零变化；优化再次冻结
- **A股 ETF 可行性回测（2026-08-27 第十九轮，DEVLOG）**：`tests/ashare_data.py`（东财 push2his 日线前复权，12 只 ETF 本地 pickle 缓存，**试验层未接生产**）+ `tests/backtest_ashare.py`（生产 1d 引擎原封不动，A 股执行口径：long-only/T+1 管理自次日/T+0 品种当日止损/跳空按开盘成交/双边 0.06% 费；预登记三关写死于 docstring）——12 ETF（50/300/500/1000/科创50/创业板/证券/半导体/红利/纳指/黄金/恒生互联网）净 +143.2R（1714 笔、胜率 66.2%、EV +0.084R）、8/12 盈利、最差 EV −0.076R，**G1/G2/G3 三关全过 → 可行**；结构性结论：盈利集中大盘蓝筹+T+0 品种，高波动主题/小盘（1000/半导体/证券/科创50）亏损，2018 −31.7R 为最大亏损年；方向准确率 48~58% 与加密一致（执行层优势跨市场复现）；**未调任何参数，未接生产引擎**；顺带修复 `services/analysis/volume.py` first-bin 越界潜伏 bug（A股平线触发，币安数据从未遇到）
- **A股预登记调参轮（2026-08-27 第二十轮，DEVLOG）**：`tests/tune_ashare.py`（A/B/C 时间折两阶段盲测；轴 volgate/bullgate/th/stop/be/texit）——**候选（stop1.5/be0.75）被 K3 最差成员守卫一票否决**（C 段池化 +52%、2023 −11.8→−3.3R 均兑现，但红利ETF −7.6 vs −6.0R 恶化）；波动率分闸（volgate=noexp）与 EMA200 牛市闸两阶段均被数据拒绝（expanded 波动期含大量盈利交易、闸门切掉盈亏两侧）；2018（调参段内）无法在不伤总利润前提下收敛；**最终 A 股配置=第十九轮生产基线原封不动**，逐年/逐 ETF 收益表格已产出供产品化决策；若未来放弃最差成员守卫须重新预登记另开一轮
- **A股独立策略研究·T+0 扩池（2026-08-27 第二十一轮，DEVLOG）**：抛开现有策略的独立研究。定调：现实对标=币圈 1d 家族（~29R/年）；T+0 三只 EV +0.165R vs T+1 +0.060R（2.7×）为核心线索。`tests/t0_overnight.py` 预登记协议执行完毕：9 只 T+0 池（美股 3/黄金 2/港股 3/日本 1）**盲测 H1/H2/H3 三关全过 → 扩池方向成立**——盲段 2.7 年 370 笔、胜率 68.9%、EV +0.161R、净 +59.6R（**年化 22.3R/年**，含 2025 黄金大年 +50.3R 繁荣成分；全时段口径 12.3R/年）、DD 9.3R、2018 仅 −5.8R（远浅于 12 ETF 池的 −31.7R）；新 6 只贡献 +96.2R > 旧 3 只 +63.2R（与第十九轮口径自洽）；**隔夜因子无证据**（NDX/GOLD 闸门覆盖迟于 A 段致选择不可区分+敏感性 −0.5%/−1.5% 方向不一，因子无害不采纳）；唯一弱成员=159920 恒生ETF（盲段 EV −0.123R）。数据层：60m 死于东财只有 128 根；ETF 日线=东财→腾讯 ifzq.gtimg 备胎自动切换（end 须横杠日期）；NDX/GOLD 驱动走本地 macro.db；东财 burst 探测触发 IP 封禁须单发慢速。配置=生产 1d 引擎原封不动；未接生产
- **美股 ETF/个股回测（2026-08-29 第三十二轮，DEVLOG）**：`tests/us_data.py`（Yahoo 经 VPN 系统代理：1d `period1=0` 全深度至上市日——`range=max` 会被下采样成月线；60m 仅 730 天 RTH 7 根/天→4h 两段制聚合 09:30-13:30+13:30-16:00 ET；adjclose 比率全复权；**Yahoo ts 秒级须×1000**）+ `tests/backtest_us.py`（生产 1d/4h 引擎原封不动；双向 T+0、gap 保守口径、双边 0.06% 主费率；预登记三关；记录缓存 key 含 harness 参数）——15 只（QQQ/SPY/DIA/IWM/7 巨头/SOXX/GLD/TLT/EEM）**1d 全历史三关全过**：净 **+1205.3R**（9674 笔、EV +0.125R、胜率 74.3%、DD 23.3R、PF 1.64）、45 个年份 44 正（唯 1982 −2.1R 小样本）、近 10 年 +425.2R、多 +938.1/空 +267.3 双向贡献、费率 0.12% 仍 +937.1R；QQQ +90.9R / SPY +57.1R；**朴素对照全面逊于引擎**（Donchian55/20 +534.7R DD 48.9R、EMA50/200 +241.5R DD 64.0R vs 引擎 DD 23.3R）→ "换策略"无必要，优势仍在执行层；方向准确率 49~60% 与币圈/A 股一致。**4h 弱可行不推荐**（730 天短窗：+74.7R、EV +0.065R、空 −19.5R 牛市逆风、费率 0.12% 仅剩 +13.5R）——美股周期选择=1d。零调参零生产改动；美股数据层为试验层未接生产
- **美股 1d 预登记调参轮（2026-08-29 第三十三轮，DEVLOG）——已采纳美股专用几何**：`tests/tune_us.py`（§7.3 新维度唯一一次调参机会已烧；净口径 0.06% 选择；折分 A40/B30/C30；六轴预定义单遍坐标下降；Phase2 A+B 重调→C 段一次性盲测三关一票否决）——C 段（2018-08 起）三关全过：候选 +383.3R vs incumbent +357.3R（K1 +7.3%）、**逐标的 15/15 全正**（incumbent EEM −3.2R→+4.2R）、K3 最差 +4.2 vs −3.2 → **美股专用几何 = depth 0.75 / stop 1.2 / be 0.5 / trail 0.5 / texit 24 / th 10 / fill 9**（生产币圈几何零改动；与币圈 1d 差异=浅回踩 0.75、跟踪 0.5、退出 24 根——美股 gap 频繁+日线趋势更长）。全史 +1356.4R（10905 笔 EV +0.124R DD 20.0R）vs incumbent +1205.3R（+12.6%）；池化年化 +30.5R/年、费率 0.12% +23.7R/年。**年/月化排序**（采纳配置全史净口径）：年化 R/年 META +5.2 > NVDA +4.5 > MSFT +4.4 > AAPL +4.4 > TSLA +4.3 > GOOGL +4.0 > AMZN +3.9 > IWM +3.9 > QQQ +3.8 > SOXX +3.4 > SPY +2.9 > TLT +2.8 > DIA +2.5 > EEM +1.6 > GLD +0.2；复利年化 %（f=1% 数学上限口径）META +5.3% ~ GLD +0.1%；**等权组合（月度再平衡 f=1%/笔）：年化 +4.1%/月均 +0.34%、复利月度 DD 4.0%、529 月期末 5.94x**。美股落地产品化时按此几何接线
- **7 币五年回测 + 分币明细（2026-08-29 第三十五轮，DEVLOG/BACKTEST §6）**：`tests/backtest_7coins.py`（推送列表币种 × 4 周期 5 年、R13 几何、1h 下界口径、每 symbol 一 spawn worker；复用 `_5y_cache_*` 缓存格式）——分币四周期合计 ETH +918.4 > SOL +906.6 > ZEC +890.3 > BTC +860.3 > BNB +856.7 > XRP +822.1 > DOGE +793.1R（最好/最差 16%）；池化 1h +3125.2R（7 币）/4h +2502.5/1d +348.0；**七币 × 逐年全正**；费后 1h @0.10% +1749.7R；复利仅1d 27.0×（+134%/年）仍为最可实现口径；四币锚点与既有文档精确吻合。**踩坑登记：kline_cache 深层并发回填对镜像 burst 静默部分失败（fetch-ok ≠ 返回根数达标）——深层批量回填须串行慢速（0.6s/页）并核对返回根数**
- **SUI 加入推送列表，8 币口径（2026-08-29 第三十六轮，DEVLOG/BACKTEST §6 八币版）**：SUI（2023-05 上市，1w 174 根不足 warmup 如实无成交）1h +300.9R（EV +0.129）/4h +226.9R（EV +0.391）/1d +31.4R（EV +0.398）＝合计 +559.3R/3.32 年→**年化 +168.3R/年与主流币同区间**，三周期 EV 全部落在其余七币区间内；8 币池化 1h +3426.1R/4h +2729.4/1d +379.4，八币×逐年全正；费后 1h @0.10% +1967.9R；复利仅1d 36.56×（+153%/年）。notify.json symbols 7→8（+SUIUSDT，部署机待同步）。**burst 回填坑第三次复现（SUI 1d fetch 只得 1000/1215）——串行补页+核对根数为深层回填标准流程**
- **美股加密相关标的回测（2026-08-30 第三十八轮，DEVLOG）**：`tests/backtest_us_crypto.py`（MSTR/COIN/MARA/RIOT/CLSK/HOOD × 1d/4h，预登记三关、零调参；1d 主口径=第三十三轮已采纳美股几何（incumbent 对照 +128.6R 亦全过）、4h=生产几何；era 切片 2020-08-01 起判关，全史只作背景；记录缓存键只含决策层参数 → 1d 两几何共享记录）——**1d 三关全过：era 合并净 +152.9R**（866 笔、胜率 78.3%、EV +0.177R、DD 6.4R、+25.2R/年、逐年全正、6/6 标的正、0.12% 费率仍 +147.2R）；分标的 R/年 CLSK +7.3 > RIOT +6.2 > HOOD +5.7 > MARA/MSTR +3.0 > COIN +2.8；多空双向正（+96.0/+56.9R）。4h（730 天窗口）+65.6R（+22.6R/年、5/6 正、费率鲁棒）只作方向参考。**诚实记录：与第三十二轮大盘池相反，币股池上朴素对照不输引擎**——DC55/20 合并 +178.8R、EMA50/200 +152.7R vs 引擎 +152.9R，但 DC DD 15.1R/EMA 14.3R vs 引擎 6.4R，且 DC/EMA 各有 2/6 标的为负（引擎 6/6 全正）；B&H MSTR +929.5%（DD 89.3%）/矿商 +78~443%（DD ~96%）——币股怪物级趋势利好朴素趋势与 B&H，引擎优势=回撤小+全正+执行一致性；美股产品化若含币股须向用户呈现该差异。方向准确率 38.8~57.1%（方向弱执行强更明显）。零生产改动；.gitignore 补美股缓存条目（三十二轮遗留）；踩坑：era B&H 回撤分母误写 /seg、合并段 DD 未按时间排序（伪 DD）——两枚输出层 bug 当日修复重跑，判关不受影响
- **第三十八轮说人话版（用户裁定入档）**：测了 6 只跟币圈最相关的美股（MSTR/Coinbase/三家矿商/罗宾汉），2020 年 8 月到 2026 年 8 月。**结论：能玩，但别指望跑赢"傻拿"**——①引擎 6 只全赚、六年每年都赚（1R=1% 本金口径，固定注额累计 +152.9%）；②但买了不动收益碾压一切：MSTR +929%、HOOD +833%、矿商 +78~443%，代价是中途要扛 57~96% 的暴跌，拿住的人凤毛麟角；③最笨的通道趋势跟随也比引擎多赚（+178.8R vs +152.9R），但它回撤大得多（15.1R vs 6.4R）且 6 只里亏 2 只——引擎赢在稳：回撤小、只只赚、不会错大的；④4 小时周期数据只有两年（Yahoo 只给 730 天），看着赚钱但样本太短，不推荐；⑤2026 年至今策略在币股上很安静（33 笔 +2.5R）。**选系统=选风险偏好**：要暴富就傻拿或简单跟趋势（先想好 90% 回撤睡不睡得着），要睡得着觉就用引擎
- **趋势策略（用户 Pine）优化轮·盲测失败不采纳（2026-08-30 第三十九轮，DEVLOG）**：用户拟将 TradingView KC 突破趋势策略接入产品并行运行+事件推送，要求先优化（可改框架/增删因子；用户拍板 8 推送币 × 1h+4h、MAR 最大化+回撤上限）。`tests/trend_opt.py`（自包含；时间折 A40/B30/C30、池化参数、空头=多头镜像、固定风险 f=1% 口径、15 轴单遍坐标下降、两阶段盲测四闸）——**基线忠实复刻第十四轮**（BTC/ETH/SOL 1h 原生口径交叉验证 OK；原配置扩到 8 币水土不服：BNB −29%/DOGE −33%、ZEC/DOGE 回撤 63~72%）；样本内 A 段 MAR 0.75→2.89，**盲段 C 双双 FAIL**（1h 平均 MAR 1.384 < 原配置 1.575，第一关挂；4h 候选过收紧、盲段每币 1~8 笔且 3 币负）→ **候选不予采纳、盲测机会已烧，同盲段不得二次窥探**。诚实记录：1h 候选盲段 8/8 全正、6/8 绝对收益高于原配置，输在 MAR 均值被原配置 ZEC 单币 5.62 抬轿——若开第二轮（换目标函数"平均总收益+单币下限"或改 walk-forward）须**重新预登记**，待用户拍板；否则此产品线终止。零生产改动
- **趋势策略第二轮·K1 挂 0.04pp 不采纳（2026-08-30 第四十轮，DEVLOG）**：用户授权方案 A（软"平均总收益+单币下限"目标）+做空全程开+砍 4h+年化对标生产 1h。`tests/trend_opt2.py`（多空全程：incumbent 激活 BTC/SOL 被注释的空头参数、候选镜像；软目标 J=mean_ann+0.5×min(0,worst)−回撤惩罚；4 段锚定 walk-forward 闸；盲段 C=2025-03-01 起）——第一版硬"全正"下限在含 2022 熊市的训练段打成 0 梯度（协议级错误，中止修正、盲段未污染）；修正后 Phase1 采纳 di_align=5+trail=6.0、WF 闸 4/4 过，Phase2 盲段 **K2~K5 全过**（8币6正/最差 −1.8% 优于 inc −5.3%/回撤 9%~12.4%/笔数 88.8）**唯独 K1 挂**：年化 +5.94% vs 需 ≥+7.45%（差 0.04pp，主因 trail=6.0 在 SOL 暴拉里回吐 +20.2%→+4.7%）。**病根量化：平均赢单仅 +0.68R、97.9% 离场=止损——"留不住利润"是结构问题非参数问题**；多空已修复（盲段多头 +81.4%/空头 −7.2%）。两轮盲测已烧，第三轮须新数据维度（衍生品过滤/新盲段）+重新预登记。零生产改动
- **趋势策略第三轮·结构改造失败、产品线终止（2026-08-30 第四十一轮，DEVLOG）**：用户拍板"不强制保本、利润最大化、与现有策略为两套"。预登记结构轴（be_mode/trail 扩 8.0）+弹性探针（去保本+宽跟踪 avgWin 0.59→2.26R、avgR ×3.5，探出保本单出只占 12%、紧跟踪才是真杀手）后重跑——盲段 K1 年化 +6.42% 未达 1.25×、K3 BNB −6.8% 差于最差守卫 −5.3%，**双挂**。**三轮终局：avgWin 修到 2.26R 但年化始终卡 +5.9~6.4%——KC 突破信号本身边际就这水平，利润被"长持仓封堵反转入场（笔数 824→360）+趋势单高回吐"两个结构出口吃掉，非执行层可挤**。沉淀资产：多空修复版 incumbent、验证过的 WF 闸、两套 harness。重启唯一前提=新数据维度/新盲段+重新预登记。零生产改动
- **趋势策略第四轮·收益第一+加仓解禁，四轮里第一次盲测 PASS（2026-08-30 第四十二轮，DEVLOG）**：用户指令改为"以收益率为第一目标，继续改"。`tests/trend_opt4.py`（加仓解禁 pyramiding 0~3、出场结构全开 giveback 0~4、**f 阶梯 1%/2%/3%**、回撤软闸 mean≤25%、K1 提到 1.5×；等价性检查 pyr=0 ≡ run_trend2 逐笔一致）——盲段（2025-03 起）**四闸全过**：年化 +22.1%（f=1%，vs 需 ≥+8.9%）、**8/8 币全正**、ZEC +130.8%/年化 +74.9%；f=2% +42.1%/DD34.7%、f=3% +60.4%/DD48.0%。采纳配置：long_short/trend=None/di_align=5/ema_gate=1000/kc_len=50/kc_mult=1.5/adx 15~100/rsi_gt=68.5/vol=None/**sl=2.5/be_mode=at2.0/trail_start=1.5/trail=6.0**/giveback=None/**pyramiding=0（数据自己选了不加仓）**/texit=200/cooldown=5。**代价必须说清楚：胜率只有 ~18%**——紧止损（2.5×ATR≈0.8~1.2% 价距）+延迟保本+宽跟踪的纯趋势单，80% 单子小亏出场、靠 18% 肥尾赚回全部；与生产引擎（79~97% 不亏钱）性格完全互补；ZEC 肥尾集中度高，"ZEC 不重演就打对折"是合理预期。四轮里第一次 PASS，**已具备产品化前提**（并行独立两套+推送文案必须带胜率警示+f 档位由用户选建议 f=1% 起步）。零生产改动
- **趋势策略第五轮·去 ZEC+对标生产 4h，PASS 但杠杆失效（2026-08-30 第四十三轮，DEVLOG）**：用户两条指令：①收益还是比生产 4h 差很多（预期至少差不多）；②ZEC 贡献太多去掉再优化。**先实测门槛再跑**：生产 4h f=3% 年化 +599~859%（BTC/ETH/SOL）——趋势策略 f=3% +60.4% 的差距是 10 倍不是 3 倍，如实告知。去 ZEC 重跑（7 币，从 R4 优胜起步）：盲段 PASS（年化 +16.1% vs 需 ≥+8.0%、7/7 全正、DOGE +41.1%/SUI +39.0% 补位），但**杠杆阶梯暴露真相——f=3% +27.9%/DD50.1%、f=5% +17.5%/DD68.9%（收益不升反降+回撤爆炸）**：去 ZEC 肥尾后这套信号不再支持"加杠杆=加收益"，风险预算上限在 f=3%。**终局判断：去单币肥尾后可持续年化 ~16~28%（f=1~3%），与生产 4h 的 ~600%（f=3%）差 20 倍——信号质地差异非调参可补**；有产品化价值（互补+盲段 PASS），但收益预期必须钉在 ~16~28%，不能对标生产引擎。零生产改动

- **凌晨下砸三单止损复盘 + 止损贴池插针假设否决（2026-08-31 第四十四轮，DEVLOG）**：BTC/BNB/DOGE 三张 4h 多单被 3.2~4.9×ATR 同步下砸齐止损（计划回放确认全按设计生成执行）——**事件本身属正常**：三币同天全止损五年 10 次≈2 次/年、5 币组合日R≤-3R≈4 次/年（`tests/cluster_stop_stat.py`）。顺势验证"止损距风险侧最近未扫池 ≤0.5×ATR 的单子是否系统性更差"（仓位建议插针提示的过滤版）：**描述性统计否决，翻篇**——贴池单 EV +0.411R vs 不贴池 +0.432R（差 -0.021R，bootstrap CI 跨零，p=0.664），P(贴池|被止损)=7.1%≈P(贴池|盈利单)=7.0%（零识别力），贴池单自赚 +125.3R、硬过滤白扔利润且 DD 不降（8.2R→8.2R），分币/分年无一致性（`tests/pool_stop_stat.py`，5 币 4h 五年 4346 笔，信号时刻无前视口径）；**本次三单按生产池规则全都不贴池（9.7~20.6×ATR）**——瀑布式砸穿非贴池插针。插针提示维持"提示不拦截"定位；零生产改动；DOGE 4h 记录缓存随本轮补齐
- **短窗口期望收益机器核算（2026-08-31 第四十五轮，DEVLOG）**：`tests/expect_window_stat.py`（纯统计）——8 币 × 1h+4h 日均 **毛 +3.37R / 净@0.10% +2.36R**（第三十六轮机器输出折算）；3.55 天窗口（周五 00:00→周一 13:09 北京）期望 **毛 +11.96R / 净 +8.38R**（1R=1% 本金口径）；波动带（四币五年 85.15h 滑动窗口 ×1.905 缩放）：毛 p10~p90 ≈ +4.6~+19.7R、整窗亏损概率毛 2.3%/净 8.8%（四币口径，8 币更低）；锚点校验：4h 四币净 +1273.9R 与第二十三轮精确一致、1h 下界 +1749.8R vs 文档 +1770R、带均值 ×1.905=+12.15R ≈ 直推 +11.96R 两路径互洽；实际对照 journal 无登记、已知凌晨集群止损 -3R 属正常尾部
- **策略本体改进复核 + Wyckoff 死代码修复轮（2026-08-31 第四十七轮，DEVLOG）——修复被数据否决，零生产改动**：用户要求复核"策略还可提升收益的地方，修改并回测"。通读复核结论：**策略本体（评分+几何+阈值+因子）合法改进空间已榨干**——贴池过滤（R44）、9 个衍生品/宏观闸门（R12）、权重网格（R12）、形态因子（R12b）、趋势/波动/阈值闸门（R11）、方向预测（~60% 天花板）、Pine 趋势线（R39~43）全部被既往数据否决；剩余合法路径=标的池扩展（§6.2 实测利润按标的近线性扩展；PYTH 第七轮纯样本外 +274.9R 验证过却不在推送列表）与新市场产品化（美股 1d/币股/A股 ETF，待用户拍板）。唯一代码级候选 Wyckoff spring/utad 死代码按 §7 预登记验证（`tests/wyckoff_fix_round.py`，钉窗 2026-08-31T03:00Z、两臂同码同 56376 决策点、六道闸一次性）：修复激活分支（8 事件，0.014%）但五年池化 Δ=−1.0R（−0.03%）不达 +2% 采纳闸（K0/K2~K6 全过、K1 独挂）→ 拒绝并 `git checkout` 还原。**工程发现（登记）**：本机 kline 缓存被活进程实时追加——回测评估窗口必须钉 end_time；正典 `_5y_cache_*` 记录（08-28 构建）与今日 K 线内容存在历史漂移（分币混合方向差、池化 +1.3~2.3%），本轮两臂现算规避，正典缓存是否重建留作待办
- **LTC 加入推送列表，9 币口径（2026-09-01 第五十轮，DEVLOG/BACKTEST §2 九币版）**：流程=第三十六轮 SUI 同款（先独立回测→入列表→backtest_7coins 扩池重算）。LTC 独立回测（`tests/backtest_ltc_sndk.py`，R13 几何、5 年窗口钉 2026-09-01、零调参纯样本外）：1h（下界）+521.5R（3898 笔、EV +0.134、97.4%）/4h +340.7R（EV +0.398）/1d +40.8R/1w +1.0R＝**合计 +904.0R/4.94 年→年化 +183.0R/年（净@0.10% +133.6）**；逐年全正（含 2022：1h +102.7/4h +78.0/1d +6.4R）；方向对称（1h 多/空 EV +0.134/+0.134 精确一致）；对比第六轮旧几何 +296.6R → R13 +904.0R（+205%），R13 重校准在纯样本外标的上兑现改善。**9 币池化**（backtest_7coins 扩 SYMBOLS8→9）：1h（下界）**+3955.3R**（31603 笔）/4h **+3052.6R**/1d **+420.2R**/1w +72.8R；分币合计 ETH +910.0 > SOL +905.5 > **LTC +904.0（第 3，1h 利润九币最高）** > ZEC +890.3 > BTC +884.1 > BNB +831.1 > XRP +822.1 > DOGE +793.1 > SUI +560.9（3.33 年）；九币×逐年全正；费后 1h @0.10% +2301.2R（保留 58%）；复利仅1d 53.55×（+179.4%/年，DD 4.3%）；LTC 分币明细与独立脚本精确一致（交叉验证锚点）。notify.json symbols 8→9（+LTCUSDT，本机已改、seeded=false 下轮静默播种，**部署机待同步**）。**踩坑登记（新）**：`kline_cache` 用 FAR（4e15）作 end_time 锚点时 `_contiguous_top_count` 要求最新 bar 在 end_time−step 内、FAR 距现在 ~12.6 万年 → 判定恒失败、并发回填死循环重抓同一页（SNDKB/LTC 首轮回填各踩一次）——**评估窗口必须钉真实 NOW_MS 不能用 FAR**（第四十七轮"钉 end_time"标准具体化；backtest_7coins 用 FAR 能跑仅因老币正典缓存已连续覆盖，本轮已统一改 NOW_MS=1788271200000）；另本轮顺带回测 SNDKB（综合成交量榜第 9 新币）：上市仅 83 天，4h/1d/1w 历史不足 warmup 如实跳过，1h 109 笔 +14.0R（EV +0.129）样本太短不下结论，半年后可复测。**同轮 BACKTEST.md 重构**：旧几何时代的 §2（LTC 第六轮）/§3（Top10 第七轮）/§4（五年+重校准第八轮）/§5（四币复利第二十九轮）已删除归档 DEVLOG，报告重整为 §0 总结/§1 口径/§2 九币主表/§3 费率/§4 复利（九币，`tests/compound_9coins.py` 刷新：仅1h 下界 f=1% 8.84e9×、仅1d 53.55×、共享预算 2.19e20×、月度分布/容量轨迹九币口径）/§5 跨市场索引——单一当前口径，无新旧表并存

### 已知局限（诚实口径，UI tooltip 已标注）
- **方向胜率上限 ~60%**：任何技术组件在 2 年大样本上无法稳定超过（多轮验证）；可靠优势在执行层（回踩入场+保本管理，非亏损率 1h ~97% / 1d ~86% / 4h ~79%（第 13 轮重校准后按周期分化，4h 以胜率换 EV）），利润全部来自执行层——这是产品核心承诺，不是预测引擎
- 1w 盲测样本不足（105 笔/四币 5 年）是已声明局限；回测主口径未计手续费/滑点——实测折扣（第二十三轮）：双边 0.10% ≈0.06R/笔（止损中位 ~1.8%），1h EV −39%/4h −14%，总利润交叉点双边 0.117%（单边 0.059%）；1h 的 ~97% 非亏损率是毛口径，费后 ~80%（双边 0.10%）
- **1h 回测利润为上界（2026-08-28 第三十轮审计，用户裁定按下界呈现）**：回测执行层 `sim_outcome_fast` 同根 K 线先判目标、后判保本触发；计划语义与日记重放（journal_store）是 +beR 保本挂单先于目标成交。1h（唯一固定小目标周期）成交根一根 K 线贯穿保本线+目标线时多算 +0.175R/笔——五年 22% 的 1h 交易受影响、合计多算 +543R。**1h 下界口径（日记语义）：毛 EV +0.125R / 总利润 +1770R（vs 上界 +0.164R / +2313R）**，下界仍逐年全正且优于旧几何；下界口径下 1h 在双边 0.10% 起即被 4h 反超（原口径反超点 ~0.12%），maker 入场对 1h 更关键。**4h/1d/1w 为跟踪族两顺序等价（实测差异精确为 0），其固定注额数字不受影响；BACKTEST §5 复利节的 1h 相关行已改按下界重算**（仅1h 净 f=1% 期末 154 万×→6884×、年化 ×6.0/年；共享预算 1890 亿×→8.68 亿×、年化 65×/年——下界把 1h 复利压掉 2~4 个数量级，且使容量墙从 2023 推迟到 2024 年）。BACKTEST.md 文首 + §4.3 附表 + §4.4 附注 + §5、STRATEGY.md §4 已按此修订
- **Wyckoff spring/utad 是死代码（第三十轮审计发现；第四十七轮修复验证被数据否决，按证据维持死代码现状）**：`wyckoff.py:11-12` 区间极值含被扫描的最近 15 根自身，`wyckoff.py:32-36` spring/utad 判定恒为假——该加减分分支（decision.py:318-323）从未执行。全部历史校准在此行为下完成、与当前权重自洽。第四十七轮按 §7 预登记协议验证最小修复（spring/utad 扫描改用排除被扫描 bar 的先行区间）：修复确实激活分支（56376 决策点 8 事件，0.014%），但五年池化 Δ=−1.0R（−0.03%）不达 +2% 采纳闸（K0/K2~K6 全过、K1 独挂）→ 裁决拒绝、`git checkout` 还原。**该死代码从此不是"冻结未改"而是"验证后否决"——不要再为它开第三轮，除非有新数据维度**（`tests/wyckoff_fix_round.py` 存档）
- **回测 vs 生产的口径差（第三十轮审计定位+定量，无前视违规）**：回测记录不含 funding/OI 组件（生产实盘含，权重最高 ±20，第 12 轮已证伪其增量；derivs 历史仅 ~2.7 年故 5 年本就无法模拟）；prevDay 磁吸组件回测无/生产有（**实证 0/24 样本受影响**——PDH/PDL 池 touches=1 恒被 `pools[:8]` 截断中和）；MTF 边界一根高周期 bar 差（机制存在，但回测 spacing=4 网格相位=0 使其对回测记录覆盖率 0%）。引擎核心经审计无未来函数、指标逐值 0 偏差、决策层记录 vs 生产重算 45/45 一致、执行层 clean-room 重放 4349/4349 一致
- **工程隐患（第三十轮登记）**：`source_hash()` 只覆盖 `services/analysis/*.py`，不含 harness 参数（warmup/spacing/min_bars/fill_mult）——改这些参数不会令记录缓存失效，须手工 `--refresh`；建议后续纳入键
- 清算数据是 Gate.io 统计口径而非逐笔 feed；订单簿是快照而非流；估算强平位未计维持保证金；链上实体标签数据不可达——均如实标注，不编造
- 事件日历为本地手动维护（backend/data/events.json）
- **宏观源 Yahoo 2026-08-27 起直连 403**（curl/httpx 双通道、带 UA 均拒；VPN 系统代理下可达）：无 VPN 时宏观序列冻结在最后缓存日、macro.py 403 快速失败（15 分钟封锁窗），VPN 开启时自动经代理续更；策略评分不含宏观数据，不受影响
- **策略优化已终止**（第 11b 轮结论固化；第 13 轮为 §7.3 新数据条款下的唯一一次重开并已再次冻结，见 §7 维护规则）

- **f 峰值审计·"不爆仓 f 越大越好"证伪（2026-09-02 第五十一轮，DEVLOG）**：`tests/f_ladder.py`（复用 compound_9coins 交易流管线；9 币、R13、1h 下界、净@0.10%、窗口钉 2026-09-01；**1h/4h=2 年统一窗**——本机 K 线深度边界（XRP/ZEC/SUI/LTC 1h/4h 仅 2~3.25 年、全量重算 1h 5 年每币 30~90 分钟不可行）、1d=5 年全窗；记录缓存隔离：1h/4h→`_2y_cache_*`、1d→`_5y_cache_*_1d.pkl`，load 端严格校验 df 根数==window 键）——**模型内 CAGR 随 f 单调上升直到权益转负，"峰值"=爆仓门槛前一档**（1h+4h 共享 15%/仅4h 17.5%/仅1d 20%/仅1h 30%；峰值处 DD 94~96%、单仓名义 7~15×权益、瞬时聚合亏损 ≈200% 权益）——不可执行；**实用换算（基线 1h+4h 生产口径）：DD≤10%→f≤1%、≤20%→f≤2%、≤30%→f≤3%、模型内 RUIN@17.5%**（浅记账 DD 真实更深一档；1d 五年窗保守带宽：DD≤10%→f≤1.5%、≤20%→f≤3%、≤30%→f≤4%，穿越线 DD≥10%@2%/≥20%@3.5%/≥30%@5%/≥50%@10%、RUIN@25%；五年正典口径 f=1% DD 15.7%）。峰值高=模型把风险算浅（−1R 截断/无穿仓/浅记账）的证据，非加杠杆理由。副产品：本机补齐九币 1d 5 年当前 src 记录缓存；**跨机 1d 数据差异实锤：本机重算 65.05×/n=1136 vs 部署机正典 53.55×/n=1124（+21%/+12 笔）——待办 11 又一实例**。零生产改动

- **标的池扩展分析·链上美股+币圈四候选（2026-09-02 第五十二轮，DEVLOG）**：①币安链上美股="B 后缀家族"25 个（AAPLB/TSLAB/MSTRB/CRCLB/QQQB/SPYB…，Backed 系代币化股票/ETF），全部 2026-06-11 起上市、最老 83 天=第五十轮 SNDKB"样本太短"同处境——**全部不接受，最早 2026-12 复测**（1h 描述性 23/25 正 EV 仅作记录）；②TRX/LINK/ADA/PYTH 完整 SUI 流程补档（`tests/backtest_newcands.py`，R13 零调参纯样本外、TRX/LINK/ADA 5 年+PYTH 上市起 2.52 年、1h 下界、净费单列、记录缓存 `_cand_cache_*` window 键防假窗口）：四周期合计 LINK +976.2 > ADA +924.1 > TRX +865.1 > PYTH +445.6R（2.52 年）——**年化 +175~+198R/年全部在九币区间内或之上（LINK 十一币口径第一）、含 2022 逐年全正、多空对称、EV 全部在带内或之上，四标的全部过 SUI 门——用户拍板（同轮）：只加 LINK/ADA（9→11 币）落地**：notify.json symbols 9→11（本机已改、部署机待同步）+ **routers/notify.py symbols 上限 10→20（11 币超旧 POST 校验，本轮唯一生产改动）** + BACKTEST §0/§2/§3 十一币加法口径增补（`tests/_add_link_ada.py`：池化 1h 毛 +4970%/+1006%/年、净@0.10% 保留 1h 60%；LINK 单币复利 2083.9×/+371.9%/年、ADA 1285.0×/+315.0%/年；§4 复利节保持九币口径待正典数据机重跑=待办 13）；TRX（1h 费后保留仅 8%，价值在 4h/+285.5R 费后）与 PYTH（无 2022 样本，同 SUI 先例如实标注）档留档未入；发现脚本 `tests/_discover_stocks.py`（现货镜像 752 对匹配+B 后缀甄别+撞名山寨排除）；本机 LINK/ADA `_5y_cache_*` 已按 W5 键落位可直跑 11 币扩池
- **成交量前20∪市值前20候选扫描（2026-09-02 第五十三轮，DEVLOG）**：`tests/backtest_top20.py`（榜单 `_rank_top20.py` 币安合约链 24h 额 + CMC 经 r.jina.ai 代理；甄别 `_probe_cands.py` 可交易性/深度；16 币 R13 零调参纯样本外、1h 下界、NOW_MS 与五十二轮同窗复用 `_cand_cache_*`）——**8 币过 SUI 门：AAVE +191.5 > NEAR +186.6 > AVAX +186.2 > XLM +184.9 > BCH +175.1 > FIL +171.6 > UNI +171.1 R/年（满 5 年含 2022 全正、多空对称、EV 带内、1h 费后保留 58~75% 无 TRX 型病例）+ ARB +181.1（3.39 年无 2022 如实标注）；合计毛 +6872.6R/36187 笔/+1448.1R 年**。否决/留观：ENA（+184.0，样本 2.36 年差预登记 2.4 年门约 15 天，其余全过=裁量案例）、ACE（4h 多头 EV +0.548 出带）、HEMI 0.88 年/TRUMP 1.56 年（样本短+多空不对称）；不可入：HYPE（市值#9 无币安对）/CC/LEO/XMR 已下架、GRAM 62 天深度门（最早 2027-01 复测）；SHIB(#28)/PEPE(额#24) 前二十净外。**用户拍板（同轮）：全加 8 币落地（11→19 币）**——notify.json symbols 11→19（本机已改 enabled=false、部署机待同步**须用 POST /api/notify 应用或手改后重置 seeded=false+清 seenPlans/pushedPlans，否则新币首轮会推一批【新】事件**）、BACKTEST §0/§2/§3 十九币加法口径增补（`tests/_add_top20.py`：19 币池化 1h 毛 +8643%/+1781%/年、4h +6592%/+1390%/年、1d +938%/+222%/年；净@0.10% 保留 1h 64%/4h 89%/1d 96%；8 币单币复利 AAVE 2039.7×/NEAR 2025.4× 最高、BCH 566.0×（maxDD 12.6% 全池最深））；ENA 档留观（2026-09-17 前后样本过 2.4 年自动达标可再议）
- **波动率极值追单拦截（2026-09-02 第五十六轮）——上线同日被数据证伪、用户拍板撤销、代码已回退（DEVLOG）**：触发场景=expanded 波动率状态下 4h 抛物线顶端回踩多单被反转大阴线止损（执行层无误，regime 盲区）；用户先拍板"直接拦截不警示"（`decision._build_trade_plan` 加 expanded+贴顶/底拦截），同日补跑 19 币×5 年两臂对照（`tests/chase_gate_impact.py`，`_chase_cache_*` 记录缓存）量出代价：**4h 池化 Δ+0.7R（5 年只拦 3 笔）≈ 无影响；1h Δ−1208.7R（拦掉 10608 笔、胜率 98.4%、EV +0.136R/笔、逐年全正）——拦截是净负收益规则**（被拦的主要是贴区间底的高胜率空单）。用户据此拍板"不拦截"，`decision.py` 已回退到无拦截状态（tradePlan 生成不含 regime 拦截）；脚本与缓存留档备查。**教训归档（与第四十四轮贴池过滤、第二十轮 A 股波动率分闸同一家族的第三次数据否决）**：expanded 波动率下"贴区间极端的顺势单"在本系统是利润来源而非亏损源——regime 闸门会持续切掉盈亏两侧中盈利更多的那侧；该类过滤方向此后不再开新一轮（除非有新数据维度）。**附带实锤：你复盘的"推送多单 6.118"与本机同根 K 线产出空单（CVD 共振 bearish 压制 +27 评分）方向相反——跨机 CVD 漂移的又一实例（待办 11）**，拦截修不了这种跨机数据差
- **CVD 跨机口径漂移（2026-09-03 第五十八轮，架构缺陷，DEVLOG）**：`takerBuy`（K 线 index 9）在 fapi（合约）与镜像 data-api.binance.vision（现货）下语义不同——fapi=**合约**主动买入量、镜像=**现货**主动买入量。数据源降级（fapi 不可达→现货镜像，`binance.py:202-216`）会让 CVD 组件口径漂移：平时两口径大多同向，**恰恰在暴涨/暴跌这种 CVD 背离信号最有价值的极端时刻反向**（现货散户 FOMO 买、合约派发/对冲），把引擎唯一能覆盖评分方向的高置信分支 `cvd_conf`（CVD 多周期共振）稀释成 null 甚至反向。实测 UNI 4h：本机（fapi 直连 down 但经系统代理可达、走合约口径，正确）评分 +50、另一台（降级现货镜像口径）评分 +27，差 23 精确等于 cvd −14 + cvd_conf −9。**比待办 11 的"K 线漂移"严重一个量级**——K 线漂移是历史缓存内容差，这是决策信号口径错。引擎 CVD 设计意图=合约市场主动买卖压力（fapi 首选源，与 OI/资金费率/多空比同口径），现货镜像 takerBuy 只是降级凑合品。修复方向待拍板（见待办 15）

### 运行环境

**本机（C:\dev\Coin）**：
- Node v22.18.0 位于 `C:\Program Files\nodejs\`；**npm 必须用 `npm.cmd`**（npm.ps1 被执行策略禁止）；本机 registry 未被劫持，无需 dns-override 钩子
- Python venv 在 `backend/.venv`（3.13）可直接 `.\.venv\Scripts\python.exe main.py` 启动
- Vite dev 绑定 IPv6 localhost（127.0.0.1 连不上时用 http://localhost:5173）

**D:\Work\Coin（企业网机器）**：
- **启动后端前先查 8000 端口占用**（`netstat -ano | findstr :8000`）：曾发生旧会话进程残留、新进程未抢到端口、UI 全天由旧几何服务的事故（2026-08-25 第十一轮踩坑）。后台方式启动：`Start-Process -FilePath "D:\Work\Coin\backend\.venv\Scripts\python.exe" -ArgumentList "main.py" -WorkingDirectory "D:\Work\Coin\backend" -WindowStyle Hidden -RedirectStandardError "D:\Work\Coin\backend\data\uvicorn-8000.log"`（注意 venv launcher 会带一个同名子进程，杀进程时父子都杀）
- **网络实测（2026-08-27，会随运营商策略变化，一切以运行时探测为准）**：fapi.binance.com **被 DNS 污染**（解析到假 IP 轮换：104.244.43.231/69.63.186.30/2001::…，TCP 超时；08-24 时还可达）——K线/订单簿/衍生品/ticker 自动降级镜像+Gate.io，实测降级链全通（orderbook→gateio_perp、derivatives→gateio、analysis 走镜像 K线）；data-api.binance.vision / api.gateio.ws（全端点）/ mempool.space / api.blockchain.info 均 ✅；Yahoo query1/2 一律 403（宏观冻结，见已知局限）。币安合约独有端点（premiumIndex、futures/data 系列）污染期间无镜像，衍生品数据由 Gate contract_stats 等价覆盖、无实质缺失
- **VPN（系统代理模式）实测（2026-08-27）**：用户 VPN 客户端以系统代理工作（注册表 ProxyEnable=1 + ProxyServer=127.0.0.1:13059，非 TUN 全局接管——curl/python 直连不走它，需显式 -x/代码支持）。经代理：**fapi 全端点（含 premiumIndex/futures/data）+ Yahoo 全部 200**（出口 IP 104.234.240.117）。后端已按第十六轮改造自动探测并利用系统代理：VPN 开启时 derivatives 恢复 binance 主源（真实 OI ~$8.3B vs Gate ~$4.6B）、宏观自动续更；VPN 关闭自动回退降级链，无需任何配置
- **外网访问拓扑（2026-08-25 实测）**：本机 → TL-WDR5620(192.168.0.1) → 电信"超级家庭网关"光猫(192.168.1.1，光猫路由模式) → 运营商 CGNAT（tracert 第 3 跳 10.220.224.1 私网段）——**IPv4 端口转发不可行**（宽带线路无公网 v4，除非找 ISP 申请）；**已验证可行的外网路径是 Cloudflare Tunnel**（`tools\cloudflared.exe tunnel --url http://localhost:5173 --protocol http2 --no-autoupdate`，免账号 quick tunnel，手机任意网络访问 `https://xxx.trycloudflare.com`；注意 URL 每次重启随机变化、无 SLA、公开可访问勿外传；`vite.config.ts` 已配 `allowedHosts: ['.trycloudflare.com']`——Vite 6 默认校验 Host 头，不加会被 403）；备选公网 IPv6 直连（2408: 前缀、防火墙 "CoinLens Web 5173 Public" 已放行、仅限手机有 IPv6 的场景，前缀随重拨变化）。本机出网走 Zscaler 代理（曼谷出口），ipify 等查到的是代理 IP 而非线路真实 IP
- node/npm 不在系统 PATH，Node v22.14.0 位于 `D:\360se6\Application\components\Node\`（含 npm.cmd）；运行前端命令前先 `$env:Path = "D:\360se6\Application\components\Node;$env:Path"`
- Python 用 `py -3.13`；backend/.venv 已重建并装好依赖
- **npm 网络问题解法**：企业 DNS 把 `registry.npmjs.org`/`registry.npmmirror.com` 劫持到 127.0.0.1（黑洞），Zscaler 代理也封 registry，但**直连真实 IP 可通**（DNS 层劫持、IP 层未封）。解决：`frontend/tools/dns-override.cjs`（Node `--require` 钩子，仅对当前进程把两个 registry 域名映射回真实 IP）：
  ```powershell
  $env:NODE_OPTIONS='--require D:\Work\Coin\frontend\tools\dns-override.cjs'; npm.cmd install
  ```
  `start-frontend.ps1` 已内置。若未来 npm 报 ECONNREFUSED，先确认该钩子仍生效（IP 可能变化：npmmirror 用 223.5.5.5 解析、npmjs 用 8.8.8.8 解析后更新 cjs 里的 MAP）

## 6. 数据源优先级链与网络探测（跨环境自适应）

**设计原则：不把任何一台机器的探测结论写死在代码里。** 每类数据在运行时按优先级链探测：优先源短超时失败 → 主机冷却 300s（期间快速短路）→ 自动 fallback 到下一源；响应 `source` 字段标注实际来源。`GET /api/sources` 可查看全部链序与主机实时冷却状态。在任何网络环境运行无需改代码：官方可达则用官方（自动全量），不可达自动降级。

| 数据类型 | 优先级链（运行时探测） | 降级行为 |
|---|---|---|
| K线/exchangeInfo | 币安官方合约 fapi（直连→系统代理）→ 现货镜像 data-api.binance.vision（**K线降级时剥离 index 9 takerBuy → CVD 置空**，第五十九轮，CVD 只用合约口径） | 逐类缓存命中率 |
| 订单簿 | 币安官方合约 depth（直连→系统代理）→ Gate.io 合约聚合盘 → 币安现货镜像 depth | 最后档标注"现货盘，杠杆盘口可能不同" |
| 衍生品快照 | 币安官方合约统计（直连→系统代理）→ Gate.io futures+contract_stats | 字段级 null |
| 衍生品历史/分位数 | Gate.io contract_stats（含清算USD）→ 币安 futures/data（OI/费率/多空比，无清算） | derivs.db 多源按列 UPSERT 合并 |
| 清算聚合 | Gate.io contract_stats（唯一免费源） | 多空清算置空（不编造），杠杆强平位仍可用（只需价格） |
| 全市场扫描 ticker | 币安官方合约 24hr（直连→系统代理）→ 现货镜像 24hr | — |
| 链上 | mempool.space + blockchain.info（互补） | 字段级降级 |
| 宏观日线 | Yahoo chart API（直连→系统代理） | 序列冻结在最后缓存日 |
| A股 ETF 日线（**试验层，未接生产**） | 东财 push2his → 腾讯 ifzq.gtimg fqkline 备胎（`tests/t0_overnight.py` 已接自动切换） | 本地缓存；无 takerBuy → CVD 组件中性降级；60m 历史只有 ~128 根不可回测；**burst 快速请求触发东财 IP 级临时封禁（直连/代理同灭），抓取须单发慢速** |
| 美股 ETF/个股（**试验层，未接生产**） | Yahoo chart API（直连 403 → VPN 系统代理；腾讯 usQQQ 探明可达未接备胎） | `tests/us_data.py` 本地缓存；1d 必须 `period1=0` 全深度（`range=max` 被下采样成月线）、adjclose 比率全复权、**ts 秒级须×1000**；60m 仅 730 天 RTH → 4h 两段制（09:30-13:30+13:30-16:00 ET）窗口硬限制；东财美股 secid 在封禁窗内不可用 |

**C:\dev\Coin 本机（Zscaler 企业网，2026-08-24 实测，仅作参考非结论）：**
- fapi/api.binance.com：曾长期 451（区域封锁），2026-08-24 复测可达——数据源会随时间变化，以运行时探测为准
- data-api.binance.vision（K线/exchangeInfo/depth/ticker24hr）：✅
- api.gateio.ws（现货+期货+期权；order_book/contract_stats[liq_usd/top_lsr/funding]/options tickers）：✅；liquidation_orders/public_liq_orders 需 KEY 签名 ❌
- mempool.space + api.blockchain.info charts（sampled 返回 {x,y} 对象）：✅
- query1/query2.finance.yahoo.com：⚠️ 必须带浏览器 UA（否则稳定 429）+ ≥1.6s 间隔 + 双主机轮换 + 退避
- 不可达：stooq.com（服务端已下线）、blockchair（~10 请求即 430 黑名单）、bybit/okx/kucoin/coingecko/coincap/data.binance.vision；交易所净流入/稳定币流向等实体标签数据无免费可达源——产品如实置空

## 7. 维护规则（必读）

1. **文档协议**：每次推进后更新本文件（当前状态/待办/契约变更），并在 DEVLOG.md 顶部追加该轮记录（背景/做法/数据/结论）。**说人话协议（2026-08-30 用户裁定，长期有效）**：所有面向用户的描述（DEVLOG 轮次记录、AGENTS 状态更新、回测报告、对话汇报）必须**先写说人话版**——大白话、结论直给、不堆术语（范式=STRATEGY.md、AGENTS §5「说人话总览」、DEVLOG 第三十八轮「说人话」小节）；技术细节与数据表作为依据层随后保留，技术记录不删。文档双层：说人话层在前、技术层在后。说人话层的数字同样只抄机器输出（见第 9 条），允许的现算换算仅 1R=1% 本金的固定注额换算。
2. **决策权重/几何改动协议**：decision.py 权重注释记录校准依据；**改权重前必须重跑 `backend/tests/backtest_decision.py` 并检查 IS/OOS 两期一致性**。`_bt_cache.pkl` 会因 services/analysis/*.py 源码变化自动失效重算（约 8 分钟）。
3. **策略优化已终止**（2026-08-22 第 11b 轮固化；**2026-08-25 第 13 轮按新数据条款重开一次并已再次冻结**）：第 13 轮以 5 年历史（2021-08 起的调参前时代为新维度）按预登记协议（两阶段盲测、单遍坐标下降、1w 排除、>5% 盲测提升+逐币全正+最差币守卫三重验收）重校准 1h/4h/1d 几何并全部通过——这是 §7.3 条款下唯一一次合法重开。此后：**不要在无新数据维度的情况下重启参数优化**。若未来恢复：方向为加入美股/加密相关标的（MSTR/COIN/NDX，Yahoo 数据源已探测可达但需限流控制），并沿用第 13 轮协议（OOS 只看一次、坐标轴值预定义、分币种交叉验证、两阶段盲测）。
4. **回测证伪即删除**：零权重/负贡献因子从代码与 UI 删除（先例：图表形态、K线形态、factorContext chips、FVG/sweep/extension 决策分支——见 DEVLOG 12b 轮）；展示型数据（DerivativesPanel/MacroPanel）不属决策因子，保留。
5. **klinecharts v10 API 要点**（与 v9 差异，改图表代码前必读）：
   - 数据只能通过 `chart.setDataLoader({getBars, subscribeBar, unsubscribeBar})` 喂入，`applyNewData/updateData` 已删除
   - `getBars({type:'init'|'forward'|'backward', timestamp, callback})`：**forward=向左滚动加载更早历史**（timestamp=当前最旧一根，返回数据前插，endTime=timestamp-1）；backward=加载更新数据（刷新模式不用，返回空）；init 的 callback 必须传 `{forward:true}`，否则两个方向的分页都被禁用
   - `subscribeBar({callback})` 在 init 完成后自动调用——只保存 callback 供 App 刷新时 `syncBars` 用；symbol/period 变化自动 unsubscribe
   - `createIndicator(value, isStack)` 只有两个参数，pane 用 value.paneId 指定；内置 EMA/RSI/VOL 直接覆盖 calcParams（EMA [20,50,200]、RSI [14]）
   - KLineData 字段是 `timestamp`（不是 time）；自定义 overlay 用 `registerOverlay` + `createPointFigures` 返回 Figure 数组
   - 图表标注现状：**流动性池水平线与 OB/FVG 区域矩形已从图表移除**（用户要求精简；池位与区域仍在决策卡关键价位、交易计划入场区）；保留：均衡位 simpleTag、"扫↑/扫↓"、BOS/CHoCH/Wyckoff 文字标注、回放蓝色竖带
6. **图表数据流**：symbol/interval 变化 → setPeriod+setSymbol → loader.getBars 拉 analysis → overlays 按 groupId 重建；刷新 → App 重拉 analysis → `chartRef.syncBars(尾部K线)` 原地同步；K 线点击回放：`chart.subscribeAction('onCandleBarClick')`（payload `{dataIndex,data:{current}}`）→ App 以 asOf 重拉 analysis。
7. **诚实口径**：所有建议为规则化纪律提示非预测；UI 文案与 tooltip 不承诺胜率；不可达的数据如实置空。
8. **回测脚本必须并发执行**（2026-08-25 用户要求固化）：任何新写（及下次改动）的回测/决策记录计算类脚本不得纯串行长跑（wall time >10 分钟不允许）——记录计算为 CPU 密集，**必须用 multiprocessing 多进程并行（每 symbol×timeframe 一个 worker；纯 threading 受 GIL 限制无效；Windows 下入口必须 `if __name__ == "__main__"` 保护）**，K 线回填等网络段用 asyncio 并发（尊重各数据源限流，参考 kline_cache 的分页并发与重试）；各 worker 结果写独立缓存文件后主进程汇总。既有串行脚本（backtest_5y/top10/ltc 等）在下次触碰时按此标准并行化。另注意本机后台进程 ~15 分钟会被静默强杀（DEVLOG 第八轮踩坑），并发化同时是缩短总时长、规避强杀的手段。
9. **报告叙述性数字必须机器核验**（2026-08-29 第二十九轮勘误教训固化）：BACKTEST.md/STRATEGY.md/DEVLOG 中的**叙述性推导数字**（排名、单位换算、权益轨迹等非脚本输出直抄值）必须由脚本打印输出或逐项对照运行日志核验后写入，**不得心算**——助手生成的叙述文字没有执行校验，是本项目的错误高发层（先例：第二十九轮排名错位一处 + 10× 换算滑位两处，均写入两份文档，由用户质疑触发机器审计才发现；而同期代码层 bug（变量遮蔽/同 bar 进出排序）全部被运行时当场捕获，无一流入表格数字）。回测脚本应把报告会用到的叙述数字（轨迹/排名等）直接打印成输出段（先例：`compound_backtest.py` 权益轨迹与年化排名段），文档只准抄脚本输出。表格直抄值与叙述推导值的可信度是两个层级，用户质疑任何一个数字时先跑机器审计再回答。

## 8. 待办

1. **用户手机/外网验收（已通过，2026-08-25）**：Cloudflare Tunnel 外网访问实测成功（quick tunnel URL 见 DEVLOG 第十三轮；URL 随隧道重启变化，重跑隧道命令取新地址）。移动端 UI 验收（Header 两行折叠/侧栏下折/扫描弹窗近全屏）随本次外网访问一并覆盖。
2. **用户浏览器验收机构级 UI**：刷新 http://localhost:5173 复核——Header「⚡扫描」按钮、右侧栏三个 Tab、市场数据 Tab（宏观/链上/订单簿/清算）、交易 Tab（组合风控/仓位/日记）、衍生品分位数与 RR25/Max Pain、K 线点击回放
3. **用户浏览器验收仓位建议增强（2026-08-25 两轮）**：交易 Tab「我的仓位」填写开仓时间后——顶部动作横幅（最优先纪律动作）、证据状态 chip、评分漂移 chip、MFE/MAE chip、持仓期间事件列表、止盈参考阶梯表；建议项（入场质量/止损宽度/贴池插针/资金费率 carry/事件预警/早离场/跟踪收紧）
4. **用户浏览器验收盈利扩展三杠杆（2026-08-25）**：开启预警铃铛后等一次刷新——新计划/计划转向 toast（点击切标的）、当前标的回踩入场区与挂单到期提醒；交易 Tab 组合风控的每仓位紧急度 chip；交易日记的偏离成本行与离场原因拆解
5. **用户验收微信推送（2026-08-27 第十七轮）**：pushplus.plus 扫码登录复制 token → `POST /api/notify {"token":"...","enabled":true}` → `POST /api/notify/test` 收到测试消息 → 等下一个整点+5min 确认调度运行（events 模式首轮静默播种，事件从第二轮起推）。**当前状态（第二十二/二十七轮）**：部署机与本机均已配置企业微信通道且 enabled=true，intervals=["1h","4h"]——**双机冗余推送**（两机指纹独立、配置需分别改、偶发重复消息属预期）；**2026-09-02 第五十三轮起 symbols=BTC/ETH/SOL/BNB/XRP/ZEC/DOGE/SUI/LTC/LINK/ADA/AAVE/NEAR/AVAX/XLM/BCH/FIL/UNI/ARB（11→19，用户拍板"全加"8 个过门币；本机已改，部署机待同步；routers symbols 上限 10→20 第五十二轮已改）**；待用户在手机端确认收到带周期标签的事件消息即可关闭此项
6. 未排期迭代方向：AI 盘面解读（LLM 汇总各维度）、策略回测平台、可见区域成交量分布、按 interval 动态轮询周期、美股/加密相关标的（MSTR/COIN/NDX）、微信推送前端设置面板
7. 未排期（第十九轮可行 + 第二十轮调参被拒维持基线，**待用户凭收益表格决策**）：产品化（东财数据源接 `services/`、A 股视图/Tab、若接入则日志与仓位模块对 A 股品种的适配）。若未来放弃最差成员守卫换池化提升，须重新预登记另开调参轮（见 DEVLOG 第二十轮）
8. 未排期（第三十二轮美股 1d 可行、4h 不推荐；第三十三轮调参已采纳美股专用几何，**待用户决策**）：美股产品化（Yahoo 数据源接 `services/`——依赖 VPN 系统代理且直连 403；美股视图/Tab/扫描；腾讯美股备胎；几何按第三十三轮 depth 0.75/stop 1.2/be 0.5/trail 0.5/texit 24 接线）。调参机会已烧——若再调须有新数据维度并重新预登记
9. 第二十一轮已完成（盲测已烧、结论已记录）。可选后续：东财封禁窗过后 `--fetch-only` 补 4 个缺失驱动指数（SPX/N225/HSI/HSTECH，secid 届时单发核实）→ 隔夜因子若要翻案须**重新预登记另开一轮**（本轮敏感性已看，不可复用盲段）；pytdx（60m 最厚数据源）未验证，若未来做 60m 先导研究再探
10. **已终止（2026-08-30 用户拍板，第四十二/四十三轮后决定不产品化）**：用户 Pine 趋势策略优化线到此为止。最终留档（DEVLOG 第四十二/四十三轮）：两套盲测 PASS 配置——①8 币版（含 ZEC）：盲段年化 +22.1%/f=1%、8/8 正、ZEC 肥尾 +130.8%；②7 币版（去 ZEC）：盲段年化 +16.1%/f=1%、7/7 正、更干净但收益更低。共性警示：胜率 ~18%（80% 单子小亏出场、靠肥尾赚回）；f 阶梯失效（f=5% 收益倒退+回撤 69%）。**与生产引擎 4h（f=3% 年化 ~600%）差 20 倍——信号质地差异，非调参可补**。脚本 `tests/trend_opt.py / trend_opt2.py / trend_opt4.py` 与多空修复版 incumbent 存档备查；若未来重启须新数据维度+重新预登记
11. **已关闭（2026-08-31 第四十七轮裁决）**：Wyckoff spring/utad 死代码修复线——预登记验证修复被数据否决（K1 独挂），死代码按证据维持现状，不再开第三轮（除非新数据维度）。**遗留待办**：①正典 `_5y_cache_*` 记录（08-28 构建）与当前 K 线内容存在历史漂移（第四十七轮诊断，分币混合方向差、池化 +1.3~2.3%）——是否停机重建正典缓存待用户拍板（重建=全部下游报告数字按新窗口重呈现）；**第五十一轮新增跨机实例：本机 1d 重算 65.05×/n=1136 vs 部署机正典 53.55×/n=1124（+21%/+12 笔）——跨机 K 线内容差异比同机漂移大一个量级，正典重建决策时须同时定"哪台机器的数据为正典"**；②回测评估窗口钉 end_time 已成标准（活进程实时追加 kline 缓存的坑）；**第三维：记录缓存 load 端必须校验实际 df 根数==window 键（第五十一轮假窗口缓存踩坑：key 声称 43800 实际只喂了 18168 根）**
12. 未排期（第四十七轮复核确认的剩余收益路径）：标的池扩展——§6.2 实测池化利润按标的数量近线性扩展（新增 5 币贡献 1h +55%）。扩列表=notify.json 配置改动+按第三十六轮 SUI 流程补回测档，零策略改动。**进度：LTC 第五十轮已入（8→9）；LINK/ADA 第五十二轮已入（9→11）；第五十三轮用户拍板"全加"8 个过门币（11→19，落地见 DEVLOG 第五十三轮）；TRX/PYTH 档留档未入（用户取舍：TRX 1h 费后 8%、PYTH 无 2022）；ENA 档留观（2026-09-17 前后样本过 2.4 年自动达标可再议）；链上美股（B 后缀家族 25 个）最老 83 天按 SNDKB 标准全部拒绝、最早 2026-12 复测；GRAM 62 天深度门最早 2027-01 复测；TUTU 有回测档但市值未入 CMC 前 300。列表余量=1（上限 20）**

13. **19 币全表正典重跑（第五十二轮登记、第五十三轮扩围，待部署机执行）**：BACKTEST §4 复利节/逐年合并行仍为九币正典口径（本机无九币 5 年记录缓存，跨机 K 线漂移见待办 11）——部署机拉取本轮改动后择机跑 backtest_7coins 扩池（SYMBOLS 9→19，本机 LINK/ADA `_5y_cache_*` 与 8 币 `_cand_cache_*` 已落位可参照格式）+ compound_9coins 扩池，把 §2/§3/§4 刷成单源 19 币口径（消除加法口径的锚差与 net@0.05/0.06% 反推误差）
14. **自动交易测试网验收（第五十四轮上线、第五十五轮审查加固、第五十七轮实机下单验收、第六十轮 algo 迁移修复、第六十三轮探针/保证金加固、第六十四轮推导杠杆，用户操作）**：①（第五十七轮已完成）用户到 testnet.binancefuture.com 注册拿 key（只开合约交易权限的 key；实盘 key 同理**绝不开提现**）→ 配进 executor（`POST /api/executor` apiKey/apiSecret+dryRun=false 切 testnet）→「测试连接」返回 usdtWallet:5000+单向模式确认；**第五十七轮实机下单验收排掉 3 个真 bug**（`binance_trade.py` 三处 + `executor.py` 一处，见 DEVLOG 第五十七轮）：①签名请求从未带 `X-MBX-APIKEY` 头（所有验签必 `-2014`）②余额字段名 `totalWalletBalance`→`balance`（定仓基数恒 0=永远按 0 元建仓）③`_raise_api_error` 把 `marginType` 成功回执 `code:200` 当错误抛；手动挂单/撤单验证成功；symbols 11→19 币同步、f=15% 确认、`enabled:true` 开启空跑（`pushEvents:false` 防测试期微信噪音）。②（进行中）测试网空跑 2~4 周，对照模拟盘/推送核对：挂单/成交/撤改单/崩溃恢复是否与计划一致（测试网行情为假流动性，验证的是流程正确性不是成交质量；部分币种测试网可能无合约会跳过）；**空跑计时从第六十三轮修复后的 2026-09-05 08:05 周期重新起算**——09-02~03 死于币安条件单迁移（第六十轮）、09-04~05 死于探针假阴性两笔裸仓+-2019 保证金风暴（第六十三轮），两段均不计入空跑；**第六十一轮**：place_failed 同 bar 熔断已上线（BCH 单日 21 次重挂事故）；**第六十三轮**：探针二元化/保证金钳制/撤止损验证/30s 孤儿清扫四项加固上线、两笔裸仓与 FIL 残留止损已清账、08:05 周期实跑验证通过（4 个空单成对挂上）；**钱包拍板已落地（2026-09-05 用户选③）**：testnet riskPct 15→3（POST /api/executor 已应用落盘）；15% 时代的 4 张缩仓挂单（ETH/SOL/BNB/XRP 1h 空，名义额 18.0k/11.4k/0.6k/0.03k 合计锁死 ~6,000 保证金、fillBars 自然到期最长 24h）按 mode_switch 退役——闭行后 **30s 孤儿清扫实测自动撤掉全部 8 张交易所挂单**（4 入场+4 止损逐单记事件），可用保证金归位 6,005；下周期起按 3% 定仓（单笔 ~30% 钱包保证金，8 并发仍由保证金钳制兜底递减）；**第六十四轮**：单仓杠杆改按止损距离推导（cfg 上限 100；清算 ≥2×止损距离外，每仓保证金 ~1.5-3R、8 并发全额 ~3-4k 可容）——平 75/100x 被清算距离数学否决；**BNB 活仓完整管理链实盘验证通过**（成交→减半保本 +0.15R→止损提保本位→runner 目标单在场、交易所仓位精确减半）；10:05 周期撞 testnet 路由双挂按设计跳过（余额获取失败不开仓+探针失败保留跟踪，两处防护真实事件下行为正确）；**第六十五轮（验收日终审 2026-09-05 晚）**：修复后全天 15 成交/9 平仓零裸仓零清算、9 笔净 -1.08R 三方对账吻合（5 保本+2 目标+2 计划止损，7/9 非亏损）；挖出修复 SUI -2021 提位无重试 bug（保本事件不吞+30s 自愈，已在真实账户留痕 23:01:44）；观察项：同币跨周期 -4161/-4067 设置冲突（1h 持单挡 4h 新仓，干净跳过每小时重试）、本机→testnet 间歇闪断多窗口（全按探针防护处理）；③对账通过后用户拍板切实盘（dryRun=false+testnet=false+confirmLive=true，**riskPct 实盘硬闸 ≤3**——按补丁合并后状态校验拆包绕不过）；④**f=15% 已确认落地**（paper/testnet 沙盒按 15 跑给用户看实际回撤；实盘突破 ≤3 须拿测试网亲眼数据重新拍板）；⑤执行器单机运行（两台机器绝不可同开，coid 机器标签互不接管但同账户会双份下单）；⑥审查留档 SUGGESTION 4 项未修：422 回显密钥输入、跟踪 >100 根窗口截断（安全方向）、events 表无清理、死代码批次（using_testnet/cancel_all_orders/PaperBroker 四方法/runner_exit 等）——下次触碰执行器时顺手处理
15. **CVD 跨机口径漂移（2026-09-03 第五十八轮登记 → 第五十九轮已按选项 a 修复上线）**：`takerBuy` 在 fapi（合约）与现货镜像下语义不同，数据源降级会让 CVD 共振口径漂移、跨机方向反转。**修复（用户拍板选项 a）**：CVD 只用合约 takerBuy——`binance._get` 的 K 线镜像 fallback 在返回前剥离 index 9（`_strip_spot_taker_buy`），现货镜像降级时 CVD 置空/中性降级（fapi 不可达=放弃 CVD 组件），跨机口径从此一致。验证：剥离函数单测（index9→None、OHLCV 保留、短行安全）+ 降级路径端到端实证（fapi 冷却后镜像 K 线 index9 全 None、CVD 全置空）+ audit_indicators 的 CVD None-hole 专项 PASS + 全量回归。**存量口径（用户拍板不管）**：klines.db 114.7 万根历史 K 线的 takerBuy 来源已不可考（降级期落库的是现货真值非 NULL，无法区分），OHLCV 两源一致、影响局限在 CVD 分量的降级时段，不修存量。**回测口径（用户拍板暂不重算、标注）**：`_5y_cache_*`/`_cand_cache_*` 等记录缓存为旧混用口径，binance.py 变更使 src hash 失效、下次重跑 CVD 在降级时段变 null，主表数字会有出入——BACKTEST §1 已加口径差脚注。
